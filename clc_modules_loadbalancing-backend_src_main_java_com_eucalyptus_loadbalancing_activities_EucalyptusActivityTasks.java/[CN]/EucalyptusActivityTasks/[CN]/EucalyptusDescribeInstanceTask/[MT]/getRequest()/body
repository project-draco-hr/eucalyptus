{
  final DescribeInstancesType req=new DescribeInstancesType();
  final ArrayList<String> instances=Lists.newArrayList(this.instanceIds);
  if (this.verbose)   instances.add("verbose");
  req.setInstancesSet(instances);
  return req;
}
