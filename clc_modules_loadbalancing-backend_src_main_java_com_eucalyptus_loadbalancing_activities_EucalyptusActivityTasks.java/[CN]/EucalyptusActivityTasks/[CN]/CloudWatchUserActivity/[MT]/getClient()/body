{
  try {
    final DispatchingClient<CloudWatchMessage,CloudWatchBackend> client=new DispatchingClient<>(this.getUserId(),CloudWatchBackend.class);
    client.init();
    return client;
  }
 catch (  Exception ex) {
    throw Exceptions.toUndeclared(ex);
  }
}
