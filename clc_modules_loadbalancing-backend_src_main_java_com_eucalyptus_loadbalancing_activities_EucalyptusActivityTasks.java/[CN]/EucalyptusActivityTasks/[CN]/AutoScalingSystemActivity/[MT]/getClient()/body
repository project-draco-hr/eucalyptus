{
  try {
    final DispatchingClient<AutoScalingMessage,AutoScalingBackend> client=new DispatchingClient<>(this.getUserId(),AutoScalingBackend.class);
    client.init();
    return client;
  }
 catch (  Exception ex) {
    throw Exceptions.toUndeclared(ex);
  }
}
