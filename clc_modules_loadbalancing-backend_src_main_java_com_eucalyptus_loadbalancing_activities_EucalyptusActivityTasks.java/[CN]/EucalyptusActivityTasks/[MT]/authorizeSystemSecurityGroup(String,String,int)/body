{
  final EucalyptusAuthorizeIngressRuleTask task=new EucalyptusAuthorizeIngressRuleTask(groupNameOrId,protocol,portNum);
  checkResult(task,new EucalyptusSystemActivity(),String.format("failed to authorize:%s, %s, %d ",groupNameOrId,protocol,portNum));
}
