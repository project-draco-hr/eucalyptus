{
  final EucalyptusDescribeSecurityGroupTask task=new EucalyptusDescribeSecurityGroupTask(null,null,Lists.newArrayList(groupNameFilter),vpcId);
  return resultOf(task,new EucalyptusUserActivity(accountFullName),"failed to describe security groups");
}
