{
  return Bootstrap.isOperational() && enabled && Subnets.isSystemSourceAddress(source)&& RequestType.A.apply(query)&& (query.getName().subdomain(DomainNames.internalSubdomain(Eucalyptus.class)) || instanceData.equals(query.getName()));
}
