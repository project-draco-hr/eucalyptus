{
  this.canHas.lock();
  try {
    if (e instanceof Exception) {
      this.ex=(Exception)e;
    }
 else {
      this.ex=new WebServicesException(e);
    }
    this.finished.signal();
  }
  finally {
    this.canHas.unlock();
  }
}
