{
  this.canHas.lock();
  try {
    if (this.response == null && this.ex == null) {
      this.finished.await();
    }
    if (ex != null) {
      throw this.ex;
    }
    return this.response;
  }
  finally {
    this.canHas.unlock();
  }
}
