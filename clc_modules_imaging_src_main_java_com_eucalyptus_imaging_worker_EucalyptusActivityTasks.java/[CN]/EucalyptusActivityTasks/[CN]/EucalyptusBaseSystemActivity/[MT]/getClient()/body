{
  try {
    final DispatchingClient<BaseMessage,Eucalyptus> client=new DispatchingClient<>(this.getUserId(),Eucalyptus.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
