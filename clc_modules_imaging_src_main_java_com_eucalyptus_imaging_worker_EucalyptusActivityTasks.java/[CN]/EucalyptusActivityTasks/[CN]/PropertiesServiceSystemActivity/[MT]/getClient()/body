{
  try {
    final DispatchingClient<PropertiesMessage,PropertiesService> client=new DispatchingClient<>(this.getUserId(),PropertiesService.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
