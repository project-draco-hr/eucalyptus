{
  return principalName == null || (principal != null && principal.getType() == principalType && evaluateElement(matchOne(principalType.convertForUserMatching(principal.getValues()),principalName,PATTERN_MATCHER),principal.isNotPrincipal()));
}
