import tornado.ioloop
import tornado.web
import os
import io
import socket
import random
import json
import ConfigParser
from botoclcinterface import BotoClcInterface
from mockclcinterface import MockClcInterface
sessions = {}
use_mock = True
config = None
settings = {'cookie_secret': 'YzRmYThkNzU1NDU2NmE1ZjYxMDZiZDNmMzI4YmMzMmMK', 'login_url': '/login', }
application = tornado.web.Application([('/(favicon\\.ico)', tornado.web.StaticFileHandler, {'path': os.path.join(os.path.dirname(__file__), './'), }), ('/css/(.*)', tornado.web.StaticFileHandler, {'path': os.path.join(os.path.dirname(__file__), './css'), }), ('/images/(.*)', tornado.web.StaticFileHandler, {'path': os.path.join(os.path.dirname(__file__), './images'), }), ('/ec2', EC2Handler), ('/login', LoginHandler), ('/(.*)', RootHandler)], **settings)
if (__name__ == '__main__'):
    config = ConfigParser.ConfigParser()
    config.read('eui.ini')
    use_mock = config.getboolean('eui', 'usemock')
    application.listen(config.getint('eui', 'uiport'))
    tornado.ioloop.IOLoop.instance().start()
