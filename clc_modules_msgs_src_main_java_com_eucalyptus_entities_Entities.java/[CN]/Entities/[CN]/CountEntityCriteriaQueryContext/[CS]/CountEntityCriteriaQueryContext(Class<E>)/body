{
  super(entityClass,new NonNullFunction<CriteriaBuilder,CriteriaQuery<Long>>(){
    @Nonnull @Override public CriteriaQuery<Long> apply(    final CriteriaBuilder builder){
      return builder.createQuery(Long.class);
    }
  }
);
}
