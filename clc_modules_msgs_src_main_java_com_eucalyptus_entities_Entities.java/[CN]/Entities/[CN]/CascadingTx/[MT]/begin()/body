{
  try {
    this.txState.begin();
  }
 catch (  final RecoverablePersistenceException ex) {
    removeTransaction(this);
  }
catch (  final RuntimeException ex) {
    removeTransaction(this);
    throw ex;
  }
}
