{
  Map<String,EntityWrapper<?>> dbs=tl.get();
  if (!dbs.isEmpty()) {
    for (    EntityWrapper<?> db : dbs.values()) {
      if (db.isActive()) {
        db.commit();
      }
    }
  }
  tl.remove();
}
