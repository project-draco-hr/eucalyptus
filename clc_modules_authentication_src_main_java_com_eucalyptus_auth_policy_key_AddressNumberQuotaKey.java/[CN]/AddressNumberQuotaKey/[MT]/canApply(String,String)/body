{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_EC2,PolicySpec.EC2_ALLOCATEADDRESS).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_EC2,PolicySpec.EC2_RESOURCE_ADDRESS).equals(resourceType)) {
    return true;
  }
  return false;
}
