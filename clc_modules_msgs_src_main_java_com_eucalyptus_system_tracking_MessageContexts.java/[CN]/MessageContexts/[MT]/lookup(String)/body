{
  if (!correlationIds.containsKey(resourceId))   return Lists.newArrayList();
  return Lists.transform(correlationIds.get(resourceId),new Function<MessageCache,BaseMessage>(){
    @Override public BaseMessage apply(    MessageCache arg0){
      return arg0.getValue();
    }
  }
);
}
