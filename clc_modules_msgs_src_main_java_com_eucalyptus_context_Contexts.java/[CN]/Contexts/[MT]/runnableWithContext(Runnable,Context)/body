{
  return new Runnable(){
    @Override public void run(){
      final Context previously=tlContext.get();
      threadLocal(context);
      try {
        runnable.run();
      }
  finally {
        threadLocal(previously);
      }
    }
  }
;
}
