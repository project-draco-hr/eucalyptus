{
  return new CompatFunction<String,CompatFunction<String,String>>(){
    @Nullable @Override public CompatFunction<String,String> apply(    @Nullable final String prefix){
      return prepend(prefix);
    }
  }
;
}
