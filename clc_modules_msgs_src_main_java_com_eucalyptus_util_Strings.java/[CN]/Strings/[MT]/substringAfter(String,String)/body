{
  if (text != null) {
    final int index=text.indexOf(match);
    if (index < 0) {
      return "";
    }
 else {
      return text.substring(index + match.length());
    }
  }
 else {
    return null;
  }
}
