{
  try {
    broker=QueueBroker.getInstance();
    timer=new Timer(true);
    return true;
  }
 catch (  Exception ex) {
    log.error("ReportingBootstrapper failed to load",ex);
    return false;
  }
}
