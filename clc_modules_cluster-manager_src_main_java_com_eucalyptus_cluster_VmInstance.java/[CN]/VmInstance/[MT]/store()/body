{
  try {
    ListenerRegistry.getInstance().fireEvent(new InstanceEvent(this.uuid,this.instanceId,this.vmTypeInfo.getName(),this.getOwnerId(),this.getOwnerId(),this.placement,this.partition,this.networkBytes,this.blockBytes));
  }
 catch (  EventFailedException ex) {
    LOG.error(ex,ex);
  }
}
