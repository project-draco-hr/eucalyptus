{
  if (this.getNetworkConfig().getNetworkIndex() > 0 && newIndex > 0 && (VmState.RUNNING.equals(this.getRuntimeState()) || VmState.PENDING.equals(this.getRuntimeState()))) {
    this.getNetworkConfig().setNetworkIndex(newIndex);
  }
}
