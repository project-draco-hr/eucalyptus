{
  return new TreeSet<String>(Collections2.transform(this.networkRulesGroups,new Function<NetworkRulesGroup,String>(){
    @Override public String apply(    NetworkRulesGroup arg0){
      return arg0.getDisplayName();
    }
  }
));
}
