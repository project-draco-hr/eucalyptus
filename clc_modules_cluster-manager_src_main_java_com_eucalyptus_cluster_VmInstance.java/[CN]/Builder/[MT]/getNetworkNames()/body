{
  return new TreeSet<String>(Collections2.transform(this.networkGroups,new Function<NetworkGroup,String>(){
    @Override public String apply(    final NetworkGroup arg0){
      return arg0.getDisplayName();
    }
  }
));
}
