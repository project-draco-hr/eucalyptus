{
  if (DEBUG) {
    LOG.debug("<updateEntry> " + dn + ": "+ attrs);
  }
  try {
    context.modifyAttributes(dn,DirContext.REPLACE_ATTRIBUTE,attrs);
  }
 catch (  NamingException e) {
    LOG.error(e,e);
    throw new LdapException("Can not update entry " + dn + ": "+ attrs,e);
  }
}
