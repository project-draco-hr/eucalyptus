{
  if (DEBUG) {
    LOG.debug("<addEntryAttribute> " + dn + ": "+ attrs);
  }
  try {
    context.modifyAttributes(dn,DirContext.ADD_ATTRIBUTE,attrs);
  }
 catch (  NamingException e) {
    LOG.error(e,e);
    throw new LdapException("Can not add entry attribute " + dn + ": "+ attrs,e);
  }
}
