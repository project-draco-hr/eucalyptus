{
  if (Bootstrap.isOperational() && Topology.isEnabledLocally(AutoScalingBackend.class) && Topology.isEnabled(Compute.class)&& Topology.isEnabled(Eucalyptus.class)) {
    activityManager.doScaling();
  }
}
