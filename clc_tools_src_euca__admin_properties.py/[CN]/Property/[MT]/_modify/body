def _modify(self, name, value):
    try:
        result = self.euca.connection.get_object('ModifyPropertyValue', {'Name': name, 'Value': value, }, Property)
        print result
    except EC2ResponseError as ex:
        self.euca.handle_error(ex)
