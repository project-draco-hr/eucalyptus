{
  return new Callback<AOP>(){
    @Override public void fire(    final AOP aop){
      if (AutoScalingMetadatas.filterByOwner(ownerFullName).apply(aop)) {
        nested.fire(aop);
      }
    }
  }
;
}
