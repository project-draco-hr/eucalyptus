{
  this.hide();
  this.timer.cancel();
  this.setPopupPosition(x,y);
  this.setWidget(new HTML(html));
  if (delayInMillis > 0) {
    timer.schedule(delayInMillis);
  }
 else {
    Tooltip.this.show();
  }
}
