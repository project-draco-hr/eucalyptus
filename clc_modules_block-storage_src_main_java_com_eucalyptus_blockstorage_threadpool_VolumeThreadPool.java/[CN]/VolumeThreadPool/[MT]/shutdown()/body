{
  RLOCK.lock();
  try {
    if (pool != null) {
      LOG.info("Shutting down SC thread pool catering to volume creation");
      pool.shutdownNow();
      pool=null;
    }
  }
  finally {
    RLOCK.unlock();
  }
}
