{
  EntityWrapper<Volume> db=EntityWrapper.get(Volume.class);
  try {
    List<Volume> vols=db.query(new Volume(accountId,null));
    long size=0;
    for (    Volume v : vols) {
      size+=v.getSize();
    }
    db.commit();
    return size;
  }
 catch (  Exception e) {
    db.rollback();
    throw new AuthException("Failed to search volume info",e);
  }
}
