{
  for (  ComponentId c : this.getDependsLocal()) {
    try {
      if (Components.lookup(c).isRunningRemoteMode() || !Components.lookup(c).isAvailableLocally()) {
        return false;
      }
    }
 catch (    NoSuchElementException ex) {
      return false;
    }
  }
  return true;
}
