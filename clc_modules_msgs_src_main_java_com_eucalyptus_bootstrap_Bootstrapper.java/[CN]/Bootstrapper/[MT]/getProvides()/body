{
  if (!From(this.getClass()).has(Provides.class)) {
    Exceptions.eat("Bootstrap class does not specify the component which it @Provides.  Fine.  For now we pretend you had put @Provides(Any.classss) instead of System.exit(-1): " + this.getClass());
    return (Class<T>)Any.class;
  }
 else {
    return (Class<T>)From(this.getClass()).get(Provides.class).value();
  }
}
