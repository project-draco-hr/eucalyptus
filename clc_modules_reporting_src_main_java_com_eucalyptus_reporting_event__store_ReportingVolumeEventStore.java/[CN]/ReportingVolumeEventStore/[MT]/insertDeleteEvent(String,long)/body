{
  EntityWrapper<ReportingVolumeDeleteEvent> entityWrapper=EntityWrapper.get(ReportingVolumeDeleteEvent.class);
  try {
    ReportingVolumeDeleteEvent event=new ReportingVolumeDeleteEvent(uuid,timestampMs);
    entityWrapper.add(event);
    entityWrapper.commit();
    LOG.debug("Added event to db:" + event);
  }
 catch (  Exception ex) {
    LOG.error(ex);
    entityWrapper.rollback();
    throw new RuntimeException(ex);
  }
}
