{
  super(transition.getName(),transition.getOldState(),transition.getNewState());
  this.committor=new Committor<O>(){
    @Override public void commit(    O object) throws Exception {
      transition.commit(object);
    }
  }
;
}
