{
  try {
    this.axisService=Axis2ServiceBuilder.getAxisService(this);
  }
 catch (  AxisFault axisFault) {
    throw new ConnectException(axisFault,this);
  }
  for (  Object op : this.axisService.getOperationsNameList())   if (EucalyptusProperties.getDisabledOperations().contains((String)op))   this.disableOperation((String)op);
}
