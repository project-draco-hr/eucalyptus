{
  NodeInfo ret=null;
  for (  NodeCertInfo cert : nodeCerts) {
    NodeInfo newNodeInfo=new NodeInfo(cert);
    if ((ret=this.nodeMap.putIfAbsent(cert.getServiceTag(),newNodeInfo)) != null)     ret.setCerts(cert);
  }
}
