{
  return Bootstrap.isOperational() && enabled && RequestType.A.apply(query)&& query.getName().subdomain(awsDomain)&& (SPOOF_AWS_REGIONS || !awsRegionNames.contains(query.getName().getLabelString(1).replace("-","_").toUpperCase()));
}
