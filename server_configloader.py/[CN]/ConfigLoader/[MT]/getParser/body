@staticmethod
def getParser():
    parser = ConfigParser.ConfigParser()
    for config in CONFIG_FILE_LIST:
        if os.path.isfile(config):
            parser.read(config)
            return parser
