{
  boolean ret=ShouldLoad.INSTANCE.apply(config) || (Eucalyptus.class.equals(config.getComponentId().getClass()) && config.isHostLocal());
  LOG.debug("ServiceBootstrapper.shouldStart(" + config.toString() + "):"+ ret);
  return ret;
}
