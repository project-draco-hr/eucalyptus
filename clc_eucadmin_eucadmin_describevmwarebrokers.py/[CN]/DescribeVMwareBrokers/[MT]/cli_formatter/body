def cli_formatter(self, data):
    brokers = getattr(data, 'euca:registered')
    for broker in brokers:
        print ('VMWARE\t%s\t%s\t%s\t%s\t%s' % (cluster['euca:partition'], cluster['euca:name'], cluster['euca:hostName'], cluster['euca:state'], cluster['euca:detail']))
