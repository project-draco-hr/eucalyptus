def cli_formatter(self, data):
    brokers = getattr(data, 'euca:registered')
    for broker in brokers:
        print ('VMWARE\t%s\t%s\t%s\t%s\t%s' % (broker['euca:partition'], broker['euca:name'], broker['euca:hostName'], broker['euca:state'], broker['euca:detail']))
