{
  if (!(obj instanceof SearchRange)) {
    return false;
  }
  SearchRange that=(SearchRange)obj;
  if (this == that) {
    return true;
  }
  if ((this.start == that.start) && (this.length == that.length) && (this.sortField == that.sortField)&& (this.ascending == that.ascending)) {
    return true;
  }
  return false;
}
