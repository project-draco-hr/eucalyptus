{
  final EntityWrapper<VmInstance> db=Entities.get(VmInstance.class);
  try {
    db.persist(obj);
    db.commit();
  }
 catch (  final Exception ex) {
    LOG.error(ex,ex);
    db.rollback();
  }
}
