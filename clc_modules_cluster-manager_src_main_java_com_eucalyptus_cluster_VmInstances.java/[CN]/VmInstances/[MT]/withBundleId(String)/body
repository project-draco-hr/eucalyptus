{
  return new Predicate<VmInstance>(){
    @Override public boolean apply(    VmInstance vm){
      return vm.getBundleTask() != null && bundleId.equals(vm.getBundleTask().getBundleId());
    }
  }
;
}
