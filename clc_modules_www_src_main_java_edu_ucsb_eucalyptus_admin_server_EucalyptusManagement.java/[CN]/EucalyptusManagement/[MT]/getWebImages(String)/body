{
  return Lists.transform(Images.listAllImages(),new Function<Image,ImageInfoWeb>(){
    @Override public ImageInfoWeb apply(    Image i){
      return Composites.update(i,new ImageInfoWeb());
    }
  }
);
}
