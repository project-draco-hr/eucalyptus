def execute(self, sc_host, volume_id):
    try:
        reply = self.euca.connection.get_object('CloneVolume', {'Component': 'storage', 'VolumeId': volume_id, 'Host': sc_host, }, BooleanResponse)
        print reply
    except EC2ResponseError as ex:
        self.euca.handle_error(ex)
