def cli_formatter(self, data):
    services = getattr(data, 'euca:serviceStatuses')
    fmt = 'SERVICE%-15.15s\t%s.%s\t%s\t%s\t%s'
    for s in services:
        service_id = s['euca:serviceId']
        print (fmt % (service_id['euca:type'], service_id['euca:partition'], service_id['euca:name'], s['euca:localState'], s['euca:localEpoch'], service_id['euca:uri']))
