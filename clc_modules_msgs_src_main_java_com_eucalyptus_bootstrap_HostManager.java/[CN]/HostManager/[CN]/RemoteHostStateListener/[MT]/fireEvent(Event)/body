{
  if (event instanceof Hertz && ((Hertz)event).isAsserted(HOST_ADVERTISE_REMOTE)) {
    try {
      HostManager.broadcastHost();
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
    }
  }
}
