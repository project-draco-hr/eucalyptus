{
  if (doInit) {
    LOG.info("Performing first-time system init.");
    try {
      Bootstrap.initializeSystem();
      System.exit(123);
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
      System.exit(123);
    }
  }
 else {
  }
}
