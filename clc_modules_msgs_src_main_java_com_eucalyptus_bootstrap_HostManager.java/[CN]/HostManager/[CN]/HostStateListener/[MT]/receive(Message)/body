{
  if (Hosts.contains(msg.getSrc()) && Hosts.getHostInstance(msg.getSrc()).isLocalHost()) {
    return;
  }
 else {
    try {
      this.onMessage(msg);
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
    }
  }
}
