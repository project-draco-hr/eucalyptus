{
  Throwable ex=null;
  if (exs != null && exs.length > 0) {
    ex=exs[0];
  }
 else {
    ex=(T)new RuntimeException(message);
  }
  return new UndeclaredThrowableException(ex instanceof UndeclaredThrowableException ? ex.getCause() : ex,message);
}
