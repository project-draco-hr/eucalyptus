{
  Integer nextLevel=depth.get().decrementAndGet();
  if (nextLevel <= 0) {
    for (    EntityTransaction db : wrappers.get()) {
      if (db.isActive()) {
        db.commit();
      }
    }
    wrappers.remove();
    depth.remove();
  }
}
