{
  Integer nextLevel=depth.get().decrementAndGet();
  if (nextLevel <= 0) {
    for (    EntityWrapper db : wrappers.get()) {
      if (db.isActive()) {
        db.commit();
      }
    }
    wrappers.remove();
    depth.remove();
  }
}
