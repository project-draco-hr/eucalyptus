{
  return new UserInfo(userName,"","Eucalyptus Administrator",0l,Crypto.generateHashedPassword(userName),"","","","",Crypto.generateSessionToken(userName),Crypto.generateSessionToken(userName),true,true,true,true,0l);
}
