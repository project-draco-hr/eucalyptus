{
  return new UserInfo(userName,"","Eucalyptus Administrator",0l,Credentials.generateHashedPassword(userName),"","","","",Credentials.generateSessionToken(userName),Credentials.generateSessionToken(userName),true,true,true,true,0l);
}
