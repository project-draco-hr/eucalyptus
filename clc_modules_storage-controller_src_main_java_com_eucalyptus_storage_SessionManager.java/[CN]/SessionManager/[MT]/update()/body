{
  try {
    if (channel != null) {
      refresh();
    }
 else {
      connect();
    }
  }
 catch (  JSchException e) {
    LOG.error(e);
    throw new EucalyptusCloudException(e);
  }
catch (  IOException e) {
    LOG.error(e);
    throw new EucalyptusCloudException(e);
  }
}
