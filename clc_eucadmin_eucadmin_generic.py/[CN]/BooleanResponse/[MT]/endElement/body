def endElement(self, name, value, connection):
    if (name == 'euca:_return'):
        self.reply = value
    elif (name == 'Message'):
        self.error = value
    else:
        setattr(self, name, value)
