def check_for_extant_storage(self, partition, debug=False):
    obj = describeservices.DescribeServices()
    response = obj.main(filter_partition=partition, filter_type='storage', debug=debug)
    statuses = response.get('euca:DescribeServicesResponseType', {}).get('euca:serviceStatuses', [])
    return (len(statuses) > 0)
