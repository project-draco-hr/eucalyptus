{
  StringBuilder builder=new StringBuilder();
  if (this.state != null)   builder.append(this.state);
  if (!Strings.isNullOrEmpty(this.sourceHost) && !Strings.isNullOrEmpty(this.destinationHost))   builder.append(" ").append(this.sourceHost).append("->").append(this.destinationHost);
  return builder.toString();
}
