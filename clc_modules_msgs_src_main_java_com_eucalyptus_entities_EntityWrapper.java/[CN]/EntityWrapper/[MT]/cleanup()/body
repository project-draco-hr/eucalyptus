{
  try {
    if ((this.session != null) && (this.session.get() != null)) {
      this.session.clear();
    }
    this.transaction=null;
    if (this.em != null) {
      this.em.close();
    }
    this.em=null;
  }
  finally {
    this.runnable.run();
    outstanding.remove(this.txUuid);
  }
}
