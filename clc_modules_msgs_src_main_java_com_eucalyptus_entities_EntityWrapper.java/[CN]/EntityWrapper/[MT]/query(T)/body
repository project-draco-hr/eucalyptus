{
  Example qbe=Example.create(example).enableLike(MatchMode.EXACT);
  List<T> resultList=(List<T>)this.getSession().createCriteria(example.getClass()).setResultTransformer(Criteria.DISTINCT_ROOT_ENTITY).setCacheable(true).add(qbe).list();
  return Lists.newArrayList(Sets.newHashSet(resultList));
}
