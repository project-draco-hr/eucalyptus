{
  try {
    ListenerRegistry.getInstance().fireEvent(SnapShotEvent.with(actionInfo,snap.getNaturalId(),snap.getVolumeSize().longValue(),snap.getOwner(),snap.getDisplayName()));
  }
 catch (  final Exception e) {
    LOG.error(e,e);
  }
}
