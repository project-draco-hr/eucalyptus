{
  LogicalStorageManager blockStorageManager=BlockStorageManagerFactory.getBlockStorageManager();
  if (blockStorageManager != null)   blockStorageManager.checkPreconditions();
  BlockStorage.configure();
  return true;
}
