def cli_register(self):
    (options, args) = self.get_register_parser()
    if (len(args) != 1):
        self.register(options.host, port=options.port)
    else:
        self.register(options.host, name=args[0], port=options.port, partition=options.partition)
