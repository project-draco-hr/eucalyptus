{
  String diskUrl=getImageUrl(walrusUrl,diskInfo);
  String kernelUrl=kernelInfo != null ? getImageUrl(walrusUrl,kernelInfo) : null;
  String ramdiskUrl=ramdiskInfo != null ? getImageUrl(walrusUrl,ramdiskInfo) : null;
  VmImageInfo vmImgInfo=new VmImageInfo(diskInfo.getImageId(),kernelInfo == null ? null : kernelInfo.getImageId(),ramdiskInfo == null ? null : ramdiskInfo.getImageId(),diskUrl,kernelUrl,ramdiskInfo == null ? null : ramdiskUrl,productCodes);
  return vmImgInfo;
}
