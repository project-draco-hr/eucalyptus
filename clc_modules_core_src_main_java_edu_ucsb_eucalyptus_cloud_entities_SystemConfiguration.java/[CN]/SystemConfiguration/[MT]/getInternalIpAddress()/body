{
  String ipAddr=null;
  try {
    for (    String addr : Internets.getAllAddresses()) {
      ipAddr=addr;
      break;
    }
  }
 catch (  SocketException e) {
  }
  return ipAddr == null ? "127.0.0.1" : ipAddr;
}
