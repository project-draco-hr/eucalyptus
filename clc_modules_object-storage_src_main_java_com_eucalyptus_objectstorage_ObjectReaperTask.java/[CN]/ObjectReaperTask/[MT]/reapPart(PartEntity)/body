{
  try {
    Transactions.delete(part);
  }
 catch (  TransactionException e) {
    LOG.error("Unable to drop part: " + part.getBucketName() + " uploadId: "+ part.getUploadId()+ " partNumber: "+ part.getPartNumber()+ " uuid: "+ part.getUuid());
  }
}
