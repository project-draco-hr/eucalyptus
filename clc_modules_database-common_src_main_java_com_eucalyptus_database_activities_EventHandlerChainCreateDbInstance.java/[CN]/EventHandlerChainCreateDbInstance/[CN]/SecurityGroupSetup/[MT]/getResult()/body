{
  List<String> result=Lists.newArrayList();
  if (this.createdGroup != null)   result.add(this.createdGroup);
  if (this.createdGroupId != null)   result.add(this.createdGroupId);
  return result;
}
