{
  String remoteBrokerUrl=null;
  if (args.length > 0) {
    remoteBrokerUrl=args[0];
  }
  QueueBroker broker=new QueueBroker(DEFAULT_NAME,DEFAULT_URL,remoteBrokerUrl,DEFAULT_DIR);
  broker.startup();
}
