{
  long time=0;
  for (  final UsageMetricDimension usageMetricDimension : usage.values()) {
    time=usageMetricDimension.lastReportUsage == null ? time : Math.max(time,usageMetricDimension.lastReportUsage.getTimestampMs());
  }
  return time;
}
