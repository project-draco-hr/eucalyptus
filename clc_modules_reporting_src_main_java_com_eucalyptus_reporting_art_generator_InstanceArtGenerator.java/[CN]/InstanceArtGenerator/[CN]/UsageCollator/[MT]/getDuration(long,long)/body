{
  long startTime=Iterables.any(usage.values(),preUsagePredicate(beginMs)) ? beginMs : findFirstUsageTime();
  long endTime=Iterables.any(usage.values(),postUsagePredicate()) ? endMs : findLastUsageTime();
  return endTime - startTime;
}
