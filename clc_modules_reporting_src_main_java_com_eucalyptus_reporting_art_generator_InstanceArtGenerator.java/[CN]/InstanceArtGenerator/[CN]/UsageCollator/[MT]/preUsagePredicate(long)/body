{
  return new Predicate<UsageMetricDimension>(){
    @Override public boolean apply(    final UsageMetricDimension usageMetricDimension){
      return usageMetricDimension.hasPreUsage(startTime);
    }
  }
;
}
