{
  LOG.warn("starting to rollback");
  for (  EventHandler<? super T> h : reverseHandler) {
    try {
      h.rollback();
    }
 catch (    Exception ex) {
      ;
    }
  }
  LOG.info("finished rollback");
}
