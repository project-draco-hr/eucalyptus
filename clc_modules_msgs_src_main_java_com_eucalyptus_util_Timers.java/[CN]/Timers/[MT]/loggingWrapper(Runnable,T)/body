{
  return new Callable<T>(){
    @Override public T call() throws Exception {
      long start=System.currentTimeMillis();
      run.run();
      LOG.debug(run.toString() + ": completed in " + (System.currentTimeMillis() - start));
      return returnValue;
    }
  }
;
}
