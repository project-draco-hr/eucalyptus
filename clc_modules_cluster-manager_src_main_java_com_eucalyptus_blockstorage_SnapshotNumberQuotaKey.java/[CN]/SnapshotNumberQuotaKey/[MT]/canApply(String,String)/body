{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_EC2,PolicySpec.EC2_CREATESNAPSHOT).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_EC2,PolicySpec.EC2_RESOURCE_SNAPSHOT).equals(resourceType)) {
    return true;
  }
  return false;
}
