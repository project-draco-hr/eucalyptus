def is_octal_digit(char):
    '\n    Returns true if the parameter character is an octal digit between\n    0 through 7.\n    '
    if (char is None):
        return False
    return ('0' <= char <= '7')
