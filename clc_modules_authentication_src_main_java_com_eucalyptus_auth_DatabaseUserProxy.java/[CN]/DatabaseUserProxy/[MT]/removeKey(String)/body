{
  EntityWrapper<UserEntity> db=EntityWrapper.get(UserEntity.class);
  try {
    UserEntity user=db.getUnique(new UserEntity(){
{
        setId(DatabaseUserProxy.this.delegate.getId());
      }
    }
);
    AccessKeyEntity keyEntity=db.recast(AccessKeyEntity.class).getUnique(new AccessKeyEntity(){
{
        setId(keyId);
      }
    }
);
    user.getKeys().remove(keyEntity);
    db.recast(AccessKeyEntity.class).delete(keyEntity);
    db.commit();
  }
 catch (  Throwable e) {
    db.rollback();
    Debugging.logError(LOG,e,"Failed to get delete key " + keyId);
    throw new AuthException(e);
  }
}
