{
  this.networkToken=networkToken;
  StartNetworkType msg=new StartNetworkType(networkToken.getAccountNumber(),networkToken.getVlan(),networkToken.getNetworkName(),networkToken.getNetworkUuid()).regarding();
  msg.setUserId(networkToken.getAccountNumber());
  this.setRequest(msg);
}
