{
  this.networkToken=networkToken;
  StartNetworkType msg=new StartNetworkType(networkToken.getAccountId(),networkToken.getVlan(),networkToken.getNetworkName(),networkToken.getNetworkUuid()).regarding();
  msg.setUserId(networkToken.getAccountId());
  this.setRequest(msg);
}
