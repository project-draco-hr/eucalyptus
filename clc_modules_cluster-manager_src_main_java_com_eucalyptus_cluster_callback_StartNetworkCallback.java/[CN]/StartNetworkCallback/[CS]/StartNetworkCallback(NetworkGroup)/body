{
  this.networkGroup=networkGroup;
  StartNetworkType msg=new StartNetworkType(networkGroup.getOwnerUserId(),networkGroup.extantNetwork().getTag(),networkGroup.getNaturalId(),networkGroup.getNaturalId());
  this.setRequest(msg);
}
