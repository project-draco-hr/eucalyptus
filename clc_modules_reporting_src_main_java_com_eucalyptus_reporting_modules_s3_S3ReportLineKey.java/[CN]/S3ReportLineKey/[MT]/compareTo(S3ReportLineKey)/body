{
  if (groupByLabel == null) {
    return label.compareTo(other.label);
  }
 else {
    return (groupByLabel.compareTo(other.groupByLabel) == 0) ? label.compareTo(other.label) : groupByLabel.compareTo(other.groupByLabel);
  }
}
