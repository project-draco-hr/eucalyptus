{
  NetworkToken netToken=new NetworkToken(this.parent.getName(),userName,networkName,vlan);
  if (!this.availableVlans.remove(vlan))   throw new NetworkAlreadyExistsException();
  return netToken;
}
