{
  EntityWrapper<T> db;
  if (dbtl.get() != null) {
    try {
      db=(EntityWrapper<T>)dbtl.get();
    }
 catch (    Exception ex) {
      db=dbtl.get().recast((Class<T>)search.getClass());
    }
  }
 else {
    db=EntityWrapper.get(search);
    dbtl.set(db);
  }
  return db;
}
