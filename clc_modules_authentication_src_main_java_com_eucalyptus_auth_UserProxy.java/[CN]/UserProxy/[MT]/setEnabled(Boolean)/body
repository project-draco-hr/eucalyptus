{
  try {
    new _this(){
{
        new _mutator(){
          public void set(          UserEntity e){
            e.setEnabled(enabled);
          }
        }
.set();
      }
    }
;
  }
 catch (  EucalyptusCloudException e) {
    LOG.debug(e,e);
  }
}
