{
  try {
    new _this(){
{
        new _mutator(){
          public void set(          UserEntity e){
            e.setX509Certificate(cert);
          }
        }
.set();
      }
    }
;
  }
 catch (  EucalyptusCloudException e) {
    LOG.debug(e,e);
  }
}
