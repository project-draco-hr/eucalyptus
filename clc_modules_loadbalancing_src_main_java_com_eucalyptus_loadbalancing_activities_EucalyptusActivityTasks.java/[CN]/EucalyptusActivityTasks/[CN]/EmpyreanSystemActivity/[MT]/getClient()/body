{
  try {
    final DispatchingClient<EmpyreanMessage,Empyrean> client=new DispatchingClient<>(this.getUserId(),Empyrean.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
