{
  checkResult(new EucaAttachNetworkInterfaceTask(instanceId,networkInterfaceId,deviceIndex),new ComputeSystemActivity(),String.format("failed to attach network interface %s to %s at index %d",networkInterfaceId,instanceId,deviceIndex));
}
