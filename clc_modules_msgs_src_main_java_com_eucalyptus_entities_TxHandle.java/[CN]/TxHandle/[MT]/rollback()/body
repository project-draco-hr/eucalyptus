{
  try {
    if ((this.delegate != null) && this.delegate.isActive()) {
      this.delegate.rollback();
    }
  }
 catch (  final Throwable e) {
    LOG.error(e,e);
  }
 finally {
    this.cleanup();
  }
}
