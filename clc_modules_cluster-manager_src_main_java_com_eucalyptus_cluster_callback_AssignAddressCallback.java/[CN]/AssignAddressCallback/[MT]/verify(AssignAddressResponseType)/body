{
  try {
    this.updateState();
  }
 catch (  IllegalStateException e) {
    AddressCategory.unassign(address).dispatch(address.getCluster());
  }
catch (  Exception e) {
    LOG.debug(e,e);
    AddressCategory.unassign(address).dispatch(address.getCluster());
  }
}
