{
  if (user != null && !user.equals(Principals.nobodyUser())) {
    msg.setProperty("accessKeyID",user instanceof RoleUser ? ((RoleUser)user).getUserId() : Accounts.getFirstActiveAccessKeyId(user));
  }
  msg.setProperty("timeStamp",new Date());
}
