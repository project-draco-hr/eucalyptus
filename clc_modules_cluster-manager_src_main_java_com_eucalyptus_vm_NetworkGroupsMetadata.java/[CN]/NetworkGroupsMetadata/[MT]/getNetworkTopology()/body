{
  if (checkInterval()) {
    return topoString.get();
  }
 else {
    lock.lock();
    try {
      topoString.set(generateTopology());
      lastTime=System.currentTimeMillis();
      return topoString.get();
    }
  finally {
      lock.unlock();
    }
  }
}
