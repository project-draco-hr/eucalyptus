{
  final S3SnapshotKey snapshotKey=new S3SnapshotKey(key.getOwnerId(),key.getAccountId(),timeMillis);
  final S3UsageSnapshot sus;
synchronized (usageData) {
    sus=new S3UsageSnapshot(snapshotKey,usageData);
  }
  return sus;
}
