{
  String uri;
  try {
    if (NetworkUtil.testLocal(host)) {
      return this.getLocalUri();
    }
 else {
      return makeRemoteUri(host,port);
    }
  }
 catch (  Exception e) {
    return this.getLocalUri();
  }
}
