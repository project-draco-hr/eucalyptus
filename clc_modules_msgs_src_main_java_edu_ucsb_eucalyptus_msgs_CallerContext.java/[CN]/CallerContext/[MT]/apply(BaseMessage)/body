{
  message.setUserId(identity);
  if (privileged) {
    message.markPrivileged();
  }
  message.setCallerContext(new BaseCallerContext(Lists.newArrayList(Iterables.transform(evaluatedKeys.entrySet(),MapEntryToEvaluatedIamConditionKey.INSTANCE))));
}
