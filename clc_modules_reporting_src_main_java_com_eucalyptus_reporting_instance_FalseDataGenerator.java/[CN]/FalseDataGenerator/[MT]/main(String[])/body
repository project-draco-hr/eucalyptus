{
  int numInstances=(args.length > 0) ? Integer.parseInt(args[0]) : DEFAULT_NUM_INSTANCE;
  int numUsages=(args.length > 1) ? Integer.parseInt(args[1]) : DEFAULT_NUM_USAGE;
  int timeUsageApartMs=(args.length > 2) ? Integer.parseInt(args[2]) : DEFAULT_TIME_USAGE_APART;
  long maxMs=new FalseDataGenerator().generateFakeInstances(numInstances,numUsages,timeUsageApartMs);
}
