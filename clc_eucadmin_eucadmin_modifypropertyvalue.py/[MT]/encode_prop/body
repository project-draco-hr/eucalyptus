def encode_prop(param, dict, value):
    t = value.split('=')
    if (len(t) != 2):
        print ('Options must be of the form KEY=VALUE: %s' % value)
        sys.exit(1)
    dict['Name'] = t[0]
    dict['Value'] = t[1]
