{
  EntityWrapper<ReportingElasticIpCreateEvent> entityWrapper=EntityWrapper.get(ReportingElasticIpCreateEvent.class);
  try {
    ReportingElasticIpCreateEvent event=new ReportingElasticIpCreateEvent(uuid,timestampMs,userId,ip);
    entityWrapper.add(event);
    entityWrapper.commit();
    LOG.debug("Added event to db:" + event);
  }
 catch (  Exception ex) {
    LOG.error(ex);
    entityWrapper.rollback();
    throw new RuntimeException(ex);
  }
}
