{
  EntityWrapper<ReportingElasticIpDeleteEvent> entityWrapper=EntityWrapper.get(ReportingElasticIpDeleteEvent.class);
  try {
    ReportingElasticIpDeleteEvent event=new ReportingElasticIpDeleteEvent(uuid,timestampMs);
    entityWrapper.add(event);
    entityWrapper.commit();
    LOG.debug("Added event to db:" + event);
  }
 catch (  Exception ex) {
    LOG.error(ex);
    entityWrapper.rollback();
    throw new RuntimeException(ex);
  }
}
