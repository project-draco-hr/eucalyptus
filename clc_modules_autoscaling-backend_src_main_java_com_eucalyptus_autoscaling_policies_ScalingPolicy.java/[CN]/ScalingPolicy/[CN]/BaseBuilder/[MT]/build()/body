{
  final ScalingPolicy policy=ScalingPolicy.create(ownerFullName,autoScalingGroup,name,adjustmentType,scalingAdjustment);
  policy.setCooldown(cooldown);
  policy.setMinAdjustmentStep(minAdjustmentStep);
  return policy;
}
