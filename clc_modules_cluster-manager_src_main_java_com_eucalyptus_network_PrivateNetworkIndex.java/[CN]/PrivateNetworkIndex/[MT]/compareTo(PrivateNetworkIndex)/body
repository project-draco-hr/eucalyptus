{
  if (this.getExtantNetwork() != null) {
    return (this.getExtantNetwork().getTag().equals(o.getExtantNetwork().getTag()) ? this.getIndex().compareTo(o.getIndex()) : this.getExtantNetwork().compareTo(o.getExtantNetwork()));
  }
 else {
    return (o.getExtantNetwork() == null ? 0 : -1);
  }
}
