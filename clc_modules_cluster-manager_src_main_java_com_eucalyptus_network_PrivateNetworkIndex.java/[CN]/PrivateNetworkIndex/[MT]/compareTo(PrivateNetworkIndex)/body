{
  if (this.getNetwork() != null) {
    return (this.getNetwork().getTag().equals(o.getNetwork().getTag()) ? this.getIndex().compareTo(o.getIndex()) : this.getNetwork().compareTo(o.getNetwork()));
  }
 else {
    return (o.getNetwork() == null ? 0 : -1);
  }
}
