{
  return new SetReference<PrivateNetworkIndex,VmInstance>(){
    @Override public int compareTo(    PrivateNetworkIndex o){
      return bogusIndex.compareTo(o);
    }
    @Override public PrivateNetworkIndex set(    VmInstance referer) throws ResourceAllocationException {
      return bogusIndex;
    }
    @Override public PrivateNetworkIndex get(){
      return bogusIndex;
    }
    @Override public PrivateNetworkIndex abort() throws ResourceAllocationException {
      return bogusIndex;
    }
  }
;
}
