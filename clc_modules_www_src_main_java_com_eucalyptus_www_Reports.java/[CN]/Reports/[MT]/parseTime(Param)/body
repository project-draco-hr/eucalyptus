{
  Date time=new Date();
  try {
    String str=p.get();
    time=new Date(Long.parseLong(str));
  }
 catch (  IllegalArgumentException e) {
    LOG.debug(e,e);
  }
catch (  Exception e) {
    LOG.debug(e,e);
  }
  return time;
}
