{
  if (parent.getComponentId().hasDispatcher() && !parent.isLocal()) {
    try {
      parent.lookupService().getEndpoint().stop();
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
    }
  }
}
