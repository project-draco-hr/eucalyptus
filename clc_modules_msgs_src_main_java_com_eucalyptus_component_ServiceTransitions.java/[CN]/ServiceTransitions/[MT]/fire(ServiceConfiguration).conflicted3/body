{
  if (parent.getComponentId().hasDispatcher() && !parent.isLocal()) {
    try {
      parent.lookupService().getEndpoint().start();
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
    }
  }
}
