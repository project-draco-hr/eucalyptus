{
  if (parent.getComponentId().hasDispatcher() && !parent.isVmLocal()) {
    try {
      parent.lookupService().getEndpoint().start();
    }
 catch (    Exception ex) {
      LOG.error(ex,ex);
    }
  }
}
