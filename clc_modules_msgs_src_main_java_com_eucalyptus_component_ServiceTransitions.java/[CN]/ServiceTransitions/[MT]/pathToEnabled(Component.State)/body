{
switch (fromState) {
case ENABLED:
    return sequence(Component.State.ENABLED,Component.State.ENABLED);
default :
  return ObjectArrays.concat(ServiceTransitions.pathToDisabled(fromState),Component.State.ENABLED);
}
}
