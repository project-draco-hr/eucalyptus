{
synchronized (ServiceKeyStore.class) {
    if (singleton == null)     try {
      singleton=new ServiceKeyStore();
    }
 catch (    Exception e) {
      LOG.error(e,e);
    }
  }
  return singleton;
}
