{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_IAM,PolicySpec.IAM_CREATEGROUP).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_IAM,PolicySpec.IAM_RESOURCE_GROUP).equals(resourceType)) {
    return true;
  }
  return false;
}
