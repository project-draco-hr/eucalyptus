{
  final ServiceConfiguration config=new EphemeralConfiguration(ComponentIds.lookup(Identity.class),"identity","identity",URI.create(endpoints.iterator().next()));
  return AsyncRequests.sendSync(config,request);
}
