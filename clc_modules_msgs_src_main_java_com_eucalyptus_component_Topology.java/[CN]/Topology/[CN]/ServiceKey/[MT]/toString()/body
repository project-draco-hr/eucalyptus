{
  StringBuilder builder=new StringBuilder();
  builder.append("EnabledService:partition=").append(this.partition == null ? "null" : this.partition).append(":componentId=").append(this.componentId);
  return builder.toString();
}
