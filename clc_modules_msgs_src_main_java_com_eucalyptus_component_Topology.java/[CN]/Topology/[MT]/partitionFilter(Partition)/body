{
  return new Predicate<Entry<ServiceKey,ServiceConfiguration>>(){
    @Override public boolean apply(    final Entry<ServiceKey,ServiceConfiguration> arg0){
      return p.equals(arg0.getKey().getPartition());
    }
  }
;
}
