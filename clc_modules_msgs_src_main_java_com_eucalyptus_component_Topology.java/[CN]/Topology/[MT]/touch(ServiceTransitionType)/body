{
  if (!BootstrapArgs.isCloudController()) {
    Integer msgEpoch=msg.get_epoch();
    Topology.getInstance().currentEpoch=(Topology.getInstance().currentEpoch > msgEpoch) ? Topology.getInstance().currentEpoch : msgEpoch;
  }
}
