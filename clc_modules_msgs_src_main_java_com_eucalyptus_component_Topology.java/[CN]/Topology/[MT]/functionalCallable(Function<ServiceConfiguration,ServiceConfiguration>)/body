{
  return new Function<ServiceConfiguration,Callable<ServiceConfiguration>>(){
    @Override public Callable<ServiceConfiguration> apply(    final ServiceConfiguration input){
      return transitionCallable(input,function);
    }
  }
;
}
