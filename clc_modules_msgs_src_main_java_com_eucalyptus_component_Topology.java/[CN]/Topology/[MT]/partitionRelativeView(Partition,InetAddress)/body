{
  final String localhostAddr=localAddr.getHostAddress();
  return Lists.transform(Topology.getInstance().lookupPartitionView(partition),TypeMappers.lookup(ServiceConfiguration.class,ServiceId.class));
}
