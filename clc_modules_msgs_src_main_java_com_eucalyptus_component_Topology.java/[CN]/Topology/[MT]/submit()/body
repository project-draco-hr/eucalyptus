{
  try {
    Queue.INTERNAL.enqueue(new Callable<Object>(){
      public Object call(){
        try {
          return RunChecks.INSTANCE.call();
        }
  finally {
          ready.set(true);
        }
      }
    }
);
  }
 catch (  Exception ex) {
    ready.set(true);
  }
}
