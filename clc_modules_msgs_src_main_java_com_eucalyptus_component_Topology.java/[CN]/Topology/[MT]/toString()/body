{
  StringBuilder builder=new StringBuilder();
  builder.append("Topology:currentEpoch=").append(this.currentEpoch).append(":guard=").append(checkPrimary() ? "cloud" : "remote");
  return builder.toString();
}
