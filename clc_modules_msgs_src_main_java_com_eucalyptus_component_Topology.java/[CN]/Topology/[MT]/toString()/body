{
  StringBuilder builder=new StringBuilder();
  builder.append("Topology:currentEpoch=").append(this.currentEpoch).append(":guard=").append(BootstrapArgs.isCloudController() ? "cloud" : "remote");
  return builder.toString();
}
