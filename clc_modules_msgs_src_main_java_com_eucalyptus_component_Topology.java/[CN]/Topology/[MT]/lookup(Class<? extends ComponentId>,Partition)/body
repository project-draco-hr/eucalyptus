{
  final Partition partition=((maybePartition != null) && (maybePartition.length > 0) ? maybePartition[0] : null);
  return Topology.getInstance().getServices().get(ServiceKey.create(ComponentIds.lookup(compClass),partition));
}
