def authorized(self):
    try:
        sid = self.get_cookie('session-id')
    except:
        return False
    if ((not sid) or (sid not in sessions)):
        return False
    self.user_session = sessions[sid]
    return True
