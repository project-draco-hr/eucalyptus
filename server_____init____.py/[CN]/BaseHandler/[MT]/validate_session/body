def validate_session(self):
    session_id = self.get_cookie('session-id')
    if (not session_id):
        self.redirect('/')
    try:
        self.session = sessions[session_id]
    except KeyError:
        self.redirect('/')
