{
  if (serverChannel != null) {
    return;
  }
 else {
synchronized (NioServer.class) {
      if (serverChannel != null) {
        return;
      }
 else {
        serverChannel=ChannelUtil.getServerChannel();
      }
    }
  }
}
