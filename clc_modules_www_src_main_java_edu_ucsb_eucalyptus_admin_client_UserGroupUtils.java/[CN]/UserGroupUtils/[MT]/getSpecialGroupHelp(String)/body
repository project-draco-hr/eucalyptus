{
  if (UserGroupControl.GROUP_ALL.equals(name)) {
    return ("This is a special group containing all users. " + "It can not be changed or deleted.");
  }
 else   if (UserGroupControl.GROUP_DEFAULT.equals(name)) {
    return ("This is a special group. Any user that is created " + "without specifying a group belongs to this group. " + "It can not be changed or deleted");
  }
  return "";
}
