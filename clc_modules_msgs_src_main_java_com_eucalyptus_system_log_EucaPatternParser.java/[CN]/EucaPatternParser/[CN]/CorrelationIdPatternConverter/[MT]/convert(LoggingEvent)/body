{
  if (event.getProperties().containsKey("correlation-id")) {
    final String correlationId=event.getProperty("correlation-id");
    if (correlationId != null && correlationId.length() >= 36)     return correlationId.substring(0,8);
 else     return "unknown";
  }
 else   return "unknown";
}
