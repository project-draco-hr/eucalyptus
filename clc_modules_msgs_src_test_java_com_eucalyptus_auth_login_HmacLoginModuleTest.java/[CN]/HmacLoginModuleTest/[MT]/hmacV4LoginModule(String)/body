{
  return new Hmacv4LoginModule(){
    @Override protected AccessKey lookupAccessKey(    final String accessKeyId,    final Map<String,List<String>> parameters) throws AuthException {
      return accessKey(secret);
    }
    @Override protected void checkForReplay(    final String signature) throws AuthenticationException {
    }
  }
;
}
