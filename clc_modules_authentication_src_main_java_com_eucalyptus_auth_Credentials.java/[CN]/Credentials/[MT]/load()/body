{
  Credentials.init();
  for (  CertAlias c : CertAlias.values()) {
    try {
      if (!c.check())       c.init();
    }
 catch (    Exception e) {
      LOG.error(e);
      return false;
    }
  }
  return true;
}
