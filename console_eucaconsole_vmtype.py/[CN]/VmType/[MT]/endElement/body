def endElement(self, name, value, connection):
    if (name == 'euca:name'):
        self.name = value
    elif (name == 'euca:cpu'):
        self.cores = value
    elif (name == 'euca:disk'):
        self.disk = value
    elif (name == 'euca:memory'):
        self.memory = value
    else:
        setattr(self, name, value)
