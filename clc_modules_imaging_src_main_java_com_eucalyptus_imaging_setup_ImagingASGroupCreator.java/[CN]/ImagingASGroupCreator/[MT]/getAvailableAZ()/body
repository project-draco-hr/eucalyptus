{
  List<String> zones=new ArrayList<String>();
  try {
    List<ClusterInfoType> res=describeAvailabilityZones();
    for (    ClusterInfoType r : res)     zones.add(r.getZoneName());
  }
 catch (  Exception ex) {
    throw new EucalyptusCloudException("Can't get AZs");
  }
  return zones;
}
