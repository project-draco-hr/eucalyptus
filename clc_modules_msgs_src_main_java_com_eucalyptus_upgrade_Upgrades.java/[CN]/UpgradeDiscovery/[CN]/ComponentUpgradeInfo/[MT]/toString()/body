{
  return MoreObjects.toStringHelper(ComponentUpgradeInfo.class).add("component",component).add("entityUpgradesSize",entityUpgrades.size()).add("preUpgradesSize",preUpgrades.size()).add("postUpgradesSize",postUpgrades.size()).toString();
}
