{
  final Context ctx=Contexts.lookup();
  return Permissions.isAuthorized(PolicySpec.VENDOR_EC2,PolicySpec.EC2_RESOURCE_SECURITYGROUP,arg0.getName(),ctx.getAccount(),PolicySpec.requestToAction(ctx.getRequest()),ctx.getUser());
}
