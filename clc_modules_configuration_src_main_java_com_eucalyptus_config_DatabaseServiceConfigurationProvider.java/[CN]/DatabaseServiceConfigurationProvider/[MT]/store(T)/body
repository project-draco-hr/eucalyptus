{
  EntityWrapper<T> db=Configuration.getEntityWrapper();
  try {
    db.add(t);
    t=db.getUnique(t);
    db.commit();
  }
 catch (  Exception e) {
    db.rollback();
    LOG.error(e,e);
    throw new ServiceRegistrationException(e);
  }
  return t;
}
