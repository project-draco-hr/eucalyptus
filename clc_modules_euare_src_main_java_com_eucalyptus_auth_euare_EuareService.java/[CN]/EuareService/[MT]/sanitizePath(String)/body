{
  if (path == null || "".equals(path)) {
    return "/";
  }
 else   if (!"/".equals(path)) {
    if (path.endsWith("/")) {
      path=path.substring(0,path.length() - 1);
    }
  }
  return path;
}
