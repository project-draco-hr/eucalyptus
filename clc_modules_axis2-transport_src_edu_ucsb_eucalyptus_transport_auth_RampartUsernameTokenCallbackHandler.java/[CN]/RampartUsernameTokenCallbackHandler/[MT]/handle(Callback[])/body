{
  for (int i=0; i < callbacks.length; i++) {
    WSPasswordCallback pwcb=(WSPasswordCallback)callbacks[i];
    int type=pwcb.getUsage();
    if (type == WSPasswordCallback.USERNAME_TOKEN)     pwcb.setPassword("TEHPASSWORD");
 else     pwcb.setPassword(pwcb.getIdentifer());
  }
}
