{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_IAM,PolicySpec.IAM_CREATEUSER).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_IAM,PolicySpec.IAM_RESOURCE_USER).equals(resourceType)) {
    return true;
  }
  return false;
}
