{
switch (scope) {
case ACCOUNT:
    return Long.toString(ObjectStorageQuotaUtil.countBucketObjectNumber(resource) + 1);
case GROUP:
  return NOT_SUPPORTED;
case USER:
return Long.toString(ObjectStorageQuotaUtil.countBucketObjectNumber(resource) + 1);
}
throw new AuthException("Invalid scope");
}
