{
  LOG.debug("Adding new user " + user + " in account "+ account.getName());
  if (info == null) {
    LOG.warn("Empty user info for user " + user);
  }
  account.addUser(user,"/",true,true,info);
}
