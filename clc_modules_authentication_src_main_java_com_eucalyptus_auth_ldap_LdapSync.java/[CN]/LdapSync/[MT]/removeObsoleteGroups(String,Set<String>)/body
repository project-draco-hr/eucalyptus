{
  for (  String group : oldGroupSet) {
    try {
      Groups.deleteGroup(group,accountName,true);
    }
 catch (    AuthException e) {
      LOG.error(e,e);
      LOG.warn("Failed to delete group " + group + " in "+ accountName,e);
    }
  }
}
