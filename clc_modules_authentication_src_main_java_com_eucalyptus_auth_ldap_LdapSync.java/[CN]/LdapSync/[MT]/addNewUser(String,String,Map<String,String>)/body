{
  LOG.debug("Adding new user " + user + " in account "+ accountName);
  if (info == null) {
    LOG.warn("Empty user info for user " + user);
  }
  Users.addUser(user,"/",true,true,info,true,false,accountName);
}
