{
  this.canHas.readLock().lock();
  try {
    for (    Map<String,T> m : this.stateMaps.values())     if (this.activeMap.containsKey(name))     return true;
    return false;
  }
  finally {
    this.canHas.readLock().unlock();
  }
}
