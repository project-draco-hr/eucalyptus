{
  for (  VmTypeWeb vmw : vmTypes)   try {
    VmTypes.update(vmw.getName(),vmw.getCpu(),vmw.getDisk(),vmw.getMemory());
  }
 catch (  EucalyptusCloudException e) {
    throw new SerializableException(e.getMessage());
  }
}
