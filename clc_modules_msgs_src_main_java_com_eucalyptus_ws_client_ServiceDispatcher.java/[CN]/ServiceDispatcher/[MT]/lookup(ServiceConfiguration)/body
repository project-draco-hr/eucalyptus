{
  return configuration.isVmLocal() ? new LocalDispatcher(configuration,configuration.getComponentId().getLocalEndpointUri()) : new RemoteDispatcher(configuration,ServiceUris.internal(configuration));
}
