{
  return new Callback.Success<Component>(){
    @Override public void fire(    Component t){
      if (t.isAvailableLocally()) {
        t.stateMachine.transition(Transition.this);
      }
    }
  }
;
}
