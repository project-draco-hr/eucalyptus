{
  this.services.put(service.getName(),service);
  Components.register(service);
  EventRecord.caller(Component.class,config.isLocal() ? EventType.COMPONENT_SERVICE_INIT : EventType.COMPONENT_SERVICE_INIT,this.getName(),service.getName(),service.getUri().toString()).info();
  return service;
}
