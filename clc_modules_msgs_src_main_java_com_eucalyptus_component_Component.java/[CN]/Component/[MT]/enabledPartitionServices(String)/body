{
  Iterable<ServiceConfiguration> services=Iterables.filter(this.serviceRegistry.getServices(),Predicates.and(Components.Predicates.enabledService(),Components.Predicates.serviceInPartition(partitionName)));
  return Sets.newTreeSet(services);
}
