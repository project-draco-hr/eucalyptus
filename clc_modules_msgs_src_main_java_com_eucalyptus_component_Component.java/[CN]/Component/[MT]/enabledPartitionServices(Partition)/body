{
  Iterable<ServiceConfiguration> services=Iterables.filter(this.serviceRegistry.getServices(),Predicates.and(ServiceConfigurations.enabledService(),ServiceConfigurations.serviceInPartition(partition)));
  return Sets.newTreeSet(services);
}
