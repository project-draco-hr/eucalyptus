{
  Iterable<ServiceConfiguration> services=Iterables.filter(this.serviceRegistry.getServices(),Predicates.and(Services.enabledService(),Services.serviceInPartition(partition)));
  return Sets.newTreeSet(services);
}
