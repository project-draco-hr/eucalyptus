{
  return Iterables.filter(this.serviceRegistry.getServices(),new Predicate<ComplexService>(){
    @Override public boolean apply(    ComplexService arg0){
      return Component.State.ENABLED.equals(arg0.getState());
    }
  }
);
}
