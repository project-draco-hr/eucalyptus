{
  return String.format("Component %s =%s service=%s\n",this.identity.name(),(this.identity.isAvailableLocally() ? "" : "not") + "available",(this.serviceRegistry.hasLocalService() ? this.serviceRegistry.getLocalService() : "not-local"));
}
