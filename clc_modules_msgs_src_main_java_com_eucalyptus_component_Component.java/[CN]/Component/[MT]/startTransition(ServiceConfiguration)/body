{
  this.setServiceGoalState(configuration,this.serviceRegistry.getServices().size() == 1 ? State.ENABLED : State.DISABLED);
  return ServiceTransitions.transitionChain(configuration,State.NOTREADY);
}
