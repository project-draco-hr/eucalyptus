{
  EntityWrapper<UserInfo> db=new EntityWrapper<UserInfo>();
  UserInfo user=null;
  try {
    user=db.getUnique(new UserInfo(userId));
    db.commit();
  }
 catch (  Throwable t) {
    db.commit();
  }
  return user;
}
