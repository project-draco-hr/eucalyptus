{
  final String ip=VmNetworkConfig.ipOrDefault(privateAddr);
  this.getNetworkConfig().setPrivateAddress(ip);
  this.getNetworkConfig().setPrivateDnsName(dnsHostnamesEnabled() ? VmNetworkConfig.generateDnsName(ip,DomainNames.internalSubdomain()) : "");
}
