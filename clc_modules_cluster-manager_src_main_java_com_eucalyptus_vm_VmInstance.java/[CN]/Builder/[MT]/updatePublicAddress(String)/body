{
  final String ip=VmNetworkConfig.ipOrDefault(publicAddr);
  this.getNetworkConfig().setPublicAddress(ip);
  this.getNetworkConfig().setPublicDnsName(dnsHostnamesEnabled() ? VmNetworkConfig.generateDnsName(ip,DomainNames.externalSubdomain()) : "");
}
