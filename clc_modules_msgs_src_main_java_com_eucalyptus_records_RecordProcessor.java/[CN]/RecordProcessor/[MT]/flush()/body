{
  Thread ref=thread;
  finished=true;
  if (ref != null) {
    ref.interrupt();
    try {
      ref.run();
    }
 catch (    Throwable t) {
      LOG.trace("Not saving the shutdown event:  database already stopped: " + t.getMessage());
    }
  }
}
