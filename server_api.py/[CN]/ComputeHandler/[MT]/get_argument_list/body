def get_argument_list(self, name):
    ret = []
    index = 1
    val = self.get_argument(('%s.%d' % (name, index)))
    while val:
        ret.append(val)
        index = (index + 1)
        val = self.get_argument(('%s.%d' % (name, index)))
    return ret
