def main(self, **args):
    self.args.update(args)
    self.process_args()
    self.setup_query()
    self.check_zipfile()
    self.token = self.query_mysql(get_cmdstring('mysql_get_token'))
    self.get_credentials()
