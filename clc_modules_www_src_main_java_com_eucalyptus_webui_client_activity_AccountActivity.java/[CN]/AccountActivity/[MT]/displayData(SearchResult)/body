{
  cache.update(result);
  for (  SearchResultRow row : result.getRows()) {
    LOG.log(Level.INFO,"Row: " + row);
  }
  if (this.view == null) {
    showView(result);
  }
  this.view.showSearchResult(result);
}
