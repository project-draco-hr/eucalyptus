{
  Document inputSource=null;
  try {
    inputSource=ObjectStorageUtil.getManifestData(userName,imagePathParts[0],imagePathParts[1]);
  }
 catch (  EucalyptusCloudException e) {
    throw e;
  }
  return inputSource;
}
