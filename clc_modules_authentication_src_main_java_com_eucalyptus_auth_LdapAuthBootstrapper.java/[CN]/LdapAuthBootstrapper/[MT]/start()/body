{
  if (ENABLE) {
    if (!ensureLdapTree()) {
      return false;
    }
    AuthBootstrapHelper.ensureStandardGroupsExists();
    AuthBootstrapHelper.ensureAdminExists();
    loadCache();
  }
  return true;
}
