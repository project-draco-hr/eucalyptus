{
  EventRecord.here(Bootstrap.class,EventType.BOOTSTRAPPER_ADDED,"stage:",stage.toString(),this.component.getComponentId().name(),bootstrapper.getClass().getName(),"component=" + this.component.getComponentId().name()).info();
  this.disabledBootstrappers.remove(stage,bootstrapper);
  this.bootstrappers.put(stage,bootstrapper);
}
