{
  StringBuilder sb=new StringBuilder();
  sb.append(ARN_PREFIX).append(this.getVendor()).append("::").append(this.getNamespace()).append(':').append(this.userOrGroup).append(this.path);
  if (!"/".equals(this.path)) {
    sb.append('/');
  }
  sb.append(this.name);
  return sb.toString();
}
