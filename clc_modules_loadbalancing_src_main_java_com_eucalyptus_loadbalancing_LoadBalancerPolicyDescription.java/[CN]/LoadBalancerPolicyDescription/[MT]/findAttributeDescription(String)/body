{
  for (  final LoadBalancerPolicyAttributeDescriptionCoreView attrView : this.getPolicyAttributeDescription()) {
    if (attrView.getAttributeName().equals(attrName))     return LoadBalancerPolicyAtttributeDescriptionEntityTransform.INSTANCE.apply(attrView);
  }
  throw new NoSuchElementException();
}
