{
  Credentials.init();
  if (!Credentials.checkKeystore()) {
    LOG.info("Looks like this is the first time?");
    LOG.info("Generating system keys.");
    Credentials.createSystemKeys();
  }
  return false;
}
