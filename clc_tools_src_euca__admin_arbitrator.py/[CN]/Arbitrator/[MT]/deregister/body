def deregister(self, name):
    try:
        reply = self.euca.connection.get_object('DeregisterArbitrator', {'Name': name, }, BooleanResponse)
        print reply
    except EC2ResponseError as ex:
        self.euca.handle_error(ex)
