def cli_formatter(self, data):
    clusters = getattr(data, 'euca:registered')
    for cluster in clusters:
        print ('CLOUDS\t%s\t%s\t%s\t%s\t%s' % (cluster['euca:partition'], cluster['euca:name'], cluster['euca:hostName'], cluster['euca:state'], cluster['euca:detail']))
