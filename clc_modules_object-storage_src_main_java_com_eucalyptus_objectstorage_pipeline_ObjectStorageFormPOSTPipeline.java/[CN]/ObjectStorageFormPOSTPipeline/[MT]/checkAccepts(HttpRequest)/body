{
  if (super.checkAccepts(message) && message.getMethod().getName().equals(ObjectStorageProperties.HTTPVerb.POST.toString()) && ("multipart/form-data".equals(HttpHeaders.getHeader(message,HttpHeaders.Names.CONTENT_TYPE)))) {
    return true;
  }
  return false;
}
