{
  this.token=networkToken;
  StopNetworkType msg=new StopNetworkType(this.token.getAccountNumber(),this.token.getNetworkName(),this.token.getVlan()).regarding();
  msg.setUserId(this.token.getAccountNumber());
  this.setRequest(msg);
}
