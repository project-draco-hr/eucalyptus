{
  final Name soa=DomainNames.sourceOfAuthority(name);
  SOARecord soaRecord=new SOARecord(name,DClass.IN,TTL,soa,Name.fromConstantString("root." + soa),DomainNameRecords.serial(),1200L,180L,2419200L,NEGATIVE_TTL);
  return soaRecord;
}
