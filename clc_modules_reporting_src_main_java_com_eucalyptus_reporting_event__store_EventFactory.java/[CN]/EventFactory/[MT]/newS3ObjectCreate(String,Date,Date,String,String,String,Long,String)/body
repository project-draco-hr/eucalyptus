{
  final ReportingS3ObjectCreateEvent event=new ReportingS3ObjectCreateEvent(bucketName,objectKey,objectVersion,size,occurred.getTime(),userId);
  event.initialize(eventId,created);
  return event;
}
