{
  final ReportingInstanceCreateEvent event=new ReportingInstanceCreateEvent(uuid,instanceId,occurred.getTime(),instanceType,userId,availabilityZone);
  event.initialize(eventId,created);
  return event;
}
