{
  LOG.info(EventRecord.caller(ResourceToken.class,EventType.TOKEN_SUBMITTED,token.toString()));
  if (this.pendingTokens.remove(token))   this.submittedTokens.add(token);
 else   throw new NoSuchTokenException();
}
