{
  if (selected.contains(row)) {
    selected.remove(row);
  }
 else {
    setRowStatus(row,true);
  }
  for (  int r : selected) {
    setRowStatus(r,false);
  }
  selected.clear();
  selected.add(row);
}
