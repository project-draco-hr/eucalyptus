{
  if (!this.activeMap.containsKey(name))   throw new NoSuchElementException("Can't find registered object: " + name + " in "+ this.getClass().getSimpleName());
  TYPE obj=this.activeMap.remove(name);
  this.disabledMap.putIfAbsent(obj.getName(),obj);
}
