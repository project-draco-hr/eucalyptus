{
  try {
    final Account account=Accounts.lookupAccountById(accountNumber);
    return Accounts.isSystemAccount(account) && !Account.SYSTEM_ACCOUNT.equals(account.getName());
  }
 catch (  AuthException e) {
    throw Exceptions.toUndeclared(e);
  }
}
