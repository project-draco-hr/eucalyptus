{
  try (TransactionResource db=Entities.transactionFor(StackResourceEntity.class)){
    Criteria criteria=Entities.createCriteria(StackResourceEntity.class).add(Restrictions.eq("stackName",stackName)).add(Restrictions.eq("logicalResourceId",logicalResourceId));
    List<StackResourceEntity> entityList=criteria.list();
    for (    StackResourceEntity stackResourceEntity : entityList) {
      stackResourceEntity.setPhysicalResourceId(physicalResourceId);
    }
    db.commit();
  }
 }
