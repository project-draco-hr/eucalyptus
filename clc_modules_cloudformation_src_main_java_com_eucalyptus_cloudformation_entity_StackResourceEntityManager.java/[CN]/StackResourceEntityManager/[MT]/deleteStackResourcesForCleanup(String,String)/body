{
  try (TransactionResource db=Entities.transactionFor(StackResourceEntityForCleanup.class)){
    Criteria criteria=Entities.createCriteria(StackResourceEntityForCleanup.class).add(Restrictions.eq("accountId",accountId)).add(Restrictions.eq("stackId",stackId)).add(Restrictions.eq("recordDeleted",Boolean.FALSE));
    for (    StackResourceEntity stackResourceEntity : (List<StackResourceEntityForCleanup>)criteria.list()) {
      stackResourceEntity.setRecordDeleted(Boolean.TRUE);
    }
    db.commit();
  }
 }
