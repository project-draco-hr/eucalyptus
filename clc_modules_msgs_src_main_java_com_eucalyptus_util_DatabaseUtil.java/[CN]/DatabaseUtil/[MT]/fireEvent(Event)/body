{
  if (event instanceof ClockTick) {
    ClockTick e=(ClockTick)event;
    if (e.isBackEdge()) {
      DebugUtil.Times.print();
    }
 else {
      DebugUtil.Times.update();
      DebugUtil.debug();
    }
  }
}
