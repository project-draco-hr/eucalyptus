{
  EucalyptusWebBackend.App.getInstance().enableUsers(sessionId,userNames,new AsyncCallback<Void>(){
    public void onSuccess(    Void result){
      displaySelectedGroups();
    }
    public void onFailure(    Throwable caught){
      displaySelectedUsers();
      rootPanel.getPropertyPanel().showStatus("Enabling users failed! " + getFailureMessage(caught),true);
    }
  }
);
}
