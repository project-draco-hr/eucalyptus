{
  EucalyptusWebBackend.App.getInstance().addUser(sessionId,user,groupNames,new AsyncCallback<Void>(){
    public void onSuccess(    Void result){
      displaySelectedGroups();
    }
    public void onFailure(    Throwable caught){
      displaySelectedUsers();
      rootPanel.getPropertyPanel().showStatus("Adding user failed! " + getFailureMessage(caught),true);
    }
  }
);
}
