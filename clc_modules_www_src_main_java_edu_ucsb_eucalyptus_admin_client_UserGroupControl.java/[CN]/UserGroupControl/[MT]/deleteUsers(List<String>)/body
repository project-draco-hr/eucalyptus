{
  EucalyptusWebBackend.App.getInstance().deleteUsers(sessionId,userNames,new AsyncCallback<Void>(){
    public void onSuccess(    Void result){
      displaySelectedGroups();
    }
    public void onFailure(    Throwable caught){
      displaySelectedUsers();
      rootPanel.getPropertyPanel().showStatus("Deleting user failed! " + getFailureMessage(caught),true);
    }
  }
);
}
