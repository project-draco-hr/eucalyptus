{
  EucalyptusWebBackend.App.getInstance().disableUsers(sessionId,userNames,new AsyncCallback<Void>(){
    public void onSuccess(    Void result){
      displaySelectedGroups();
    }
    public void onFailure(    Throwable caught){
      displaySelectedUsers();
      rootPanel.getPropertyPanel().showStatus("Disabling users failed! " + getFailureMessage(caught),true);
    }
  }
);
}
