{
  EucalyptusWebBackend.App.getInstance().updateGroup(sessionId,group,new AsyncCallback<Void>(){
    public void onSuccess(    Void result){
      display();
    }
    public void onFailure(    Throwable caught){
      displaySelectedUsers();
      rootPanel.getPropertyPanel().showStatus("Updating group failed! " + getFailureMessage(caught),true);
    }
  }
);
}
