{
switch (scope) {
case ACCOUNT:
    return Long.toString(ObjectStorageQuotaUtil.countBucketByAccount(id) + 1);
case GROUP:
  return NOT_SUPPORTED;
case USER:
return Long.toString(ObjectStorageQuotaUtil.countBucketByUser(id) + 1);
}
throw new AuthException("Invalid scope");
}
