def cli_formatter(self, data):
    services = getattr(data, 'euca:registered')
    fmt = '%s\t%s\t%s'
    for s in services:
        print (fmt % (self.ServiceName.upper(), a['euca:name'], a['euca:partition']))
