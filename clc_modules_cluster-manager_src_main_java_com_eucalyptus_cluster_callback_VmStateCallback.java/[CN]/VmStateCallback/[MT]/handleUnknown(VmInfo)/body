{
  for (  final Optional<VmInstance.RestoreHandler> restoreHandler : VmInstance.RestoreHandler.parseList(VmInstances.UNKNOWN_INSTANCE_HANDLERS)) {
    if (restoreHandler.isPresent() && handleRestore(runVm,restoreHandler.get())) {
      break;
    }
  }
}
