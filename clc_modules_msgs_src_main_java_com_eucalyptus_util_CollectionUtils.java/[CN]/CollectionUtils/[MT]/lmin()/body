{
  return new NonNullFunction<Long,Function<Long,Long>>(){
    @Nonnull @Override public Function<Long,Long> apply(    final Long long1){
      return new Function<Long,Long>(){
        @Override public Long apply(        final Long long2){
          return Math.min(long1,long2);
        }
      }
;
    }
  }
;
}
