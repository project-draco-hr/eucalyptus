{
  return new NonNullFunction<Integer,Function<Integer,Integer>>(){
    @Nonnull @Override public Function<Integer,Integer> apply(    final Integer integer1){
      return new Function<Integer,Integer>(){
        @Override public Integer apply(        final Integer integer2){
          return Math.min(integer1,integer2);
        }
      }
;
    }
  }
;
}
