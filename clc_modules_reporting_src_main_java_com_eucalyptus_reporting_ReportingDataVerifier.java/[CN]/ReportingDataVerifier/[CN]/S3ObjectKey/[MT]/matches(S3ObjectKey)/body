{
  return this == that || !(accountNumber == null || that.accountNumber == null) && size == that.size && accountNumber.equals(that.accountNumber) && bucketName.equals(that.bucketName) && objectName.equals(that.objectName);
}
