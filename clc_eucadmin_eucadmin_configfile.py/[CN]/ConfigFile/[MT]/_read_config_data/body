def _read_config_data(self):
    fp = open(self.path)
    for line in fp.readlines():
        if (not line.startswith('#')):
            t = line.split('=', 1)
            if (len(t) == 2):
                self[t[0]] = t[1].strip('"\n ')
    fp.close()
