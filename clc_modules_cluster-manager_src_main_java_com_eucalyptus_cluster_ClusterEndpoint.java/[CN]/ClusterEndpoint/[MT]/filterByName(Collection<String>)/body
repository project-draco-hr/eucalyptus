{
  return new Predicate<Object>(){
    @Override public boolean apply(    Object region){
      return requestedIdentifiers == null || requestedIdentifiers.isEmpty() || requestedIdentifiers.contains(((Region)region).getDisplayName());
    }
  }
;
}
