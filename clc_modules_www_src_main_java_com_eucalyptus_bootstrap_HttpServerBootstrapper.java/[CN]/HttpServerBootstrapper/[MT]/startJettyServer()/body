{
  serverThread=Threads.newThread(new Runnable(){
    @Override public void run(){
      try {
        jettyServer.start();
      }
 catch (      Exception e) {
        LOG.debug(e,e);
      }
    }
  }
,"jetty");
  serverThread.start();
}
