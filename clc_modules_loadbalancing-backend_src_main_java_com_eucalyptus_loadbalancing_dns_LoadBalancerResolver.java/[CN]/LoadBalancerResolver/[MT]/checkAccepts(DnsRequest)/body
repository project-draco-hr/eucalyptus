{
  final Record query=request.getQuery();
  if (!Bootstrap.isOperational()) {
    return false;
  }
 else   if (DnsResolvers.RequestType.A.apply(query) && query.getName().subdomain(LoadBalancerDomainName.getLoadBalancerSubdomain())) {
    return true;
  }
  return false;
}
