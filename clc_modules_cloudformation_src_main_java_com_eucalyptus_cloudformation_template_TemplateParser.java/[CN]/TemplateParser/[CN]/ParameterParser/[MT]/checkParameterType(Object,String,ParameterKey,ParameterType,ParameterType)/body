{
  if (value != null && valueType != requiredType) {
    throw new ValidationErrorException("Template error: Parameter '" + name + "' "+ key+ " must be on a parameter of type "+ requiredType);
  }
}
