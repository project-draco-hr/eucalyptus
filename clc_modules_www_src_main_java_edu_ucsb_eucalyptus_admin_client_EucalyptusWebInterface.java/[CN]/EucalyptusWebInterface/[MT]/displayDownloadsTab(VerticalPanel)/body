{
  History.newItem("extras");
  VerticalPanel vpanel=new VerticalPanel();
  vpanel.setSpacing(15);
  EucalyptusWebBackend.App.getInstance().getHtmlByPath(sessionId,"extensions/store/test.html",new AsyncCallback(){
    public void onSuccess(    Object result){
      String html=(String)((List)result).get(0);
      parent.clear();
      parent.add(new HTML(html));
    }
    public void onFailure(    Throwable caught){
    }
  }
);
  parent.clear();
  parent.add(vpanel);
}
