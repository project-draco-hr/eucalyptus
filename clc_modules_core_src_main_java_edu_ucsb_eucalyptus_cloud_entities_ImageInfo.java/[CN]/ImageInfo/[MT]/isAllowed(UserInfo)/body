{
  if (user.isAdministrator() || user.getUserName().equals(this.getImageOwnerId()))   return true;
  for (  UserGroupInfo g : this.getUserGroups())   if ("all".equals(g.getName()))   return true;
  return this.getPermissions().contains(user);
}
