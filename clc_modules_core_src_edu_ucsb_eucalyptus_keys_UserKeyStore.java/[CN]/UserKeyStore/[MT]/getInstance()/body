{
synchronized (UserKeyStore.class) {
    if (singleton == null)     try {
      singleton=new UserKeyStore();
    }
 catch (    Exception e) {
      LOG.error(e);
      LOG.debug(e,e);
    }
  }
  return singleton;
}
