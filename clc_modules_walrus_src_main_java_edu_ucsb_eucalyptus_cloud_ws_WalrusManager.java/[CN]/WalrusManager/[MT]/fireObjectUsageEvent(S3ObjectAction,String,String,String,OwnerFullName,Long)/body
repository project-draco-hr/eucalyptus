{
  try {
    ListenerRegistry.getInstance().fireEvent(S3ObjectEvent.with(actionInfo,s3bUUID,bucketName,objectName,ownerFullName,sizeInBytes));
  }
 catch (  final Exception e) {
    LOG.error(e,e);
  }
}
