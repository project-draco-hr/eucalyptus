{
  try {
    final DispatchingClient<ImagingMessage,Imaging> client=new DispatchingClient<>(Accounts.lookupSystemAdmin().getUserId(),Imaging.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
