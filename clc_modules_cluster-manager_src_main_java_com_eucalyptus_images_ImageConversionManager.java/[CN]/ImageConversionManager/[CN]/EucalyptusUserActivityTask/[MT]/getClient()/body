{
  try {
    final DispatchingClient<ComputeMessage,Eucalyptus> client=new DispatchingClient<>(userId,Eucalyptus.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
