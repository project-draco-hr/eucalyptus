{
  try {
    final DispatchingClient<ObjectStorageRequestType,ObjectStorage> client=new DispatchingClient<>(Accounts.lookupSystemAdmin().getUserId(),ObjectStorage.class);
    client.init();
    return client;
  }
 catch (  Exception e) {
    throw Exceptions.toUndeclared(e);
  }
}
