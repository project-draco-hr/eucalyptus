def cli_formatter(self, data):
    nodes = getattr(data, 'euca:registered')
    fmt = 'NODE\t%s\t%s'
    for n in nodes:
        print (fmt % (n.get('euca:name', None), n.get('euca:clusterName', None)))
