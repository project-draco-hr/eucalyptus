{
  final List<ComponentId> currentComponentIds=ComponentIds.list();
  if (this.context == null) {
    return currentComponentIds;
  }
 else   if (!this.enabledCompIds.equals(currentComponentIds)) {
    return currentComponentIds;
  }
 else {
    return Lists.newArrayList();
  }
}
