{
  final int prime=31;
  int result=1;
  result=prime * result + ((sizeTimeSizeUnit == null) ? 0 : sizeTimeSizeUnit.hashCode());
  result=prime * result + ((sizeTimeTimeUnit == null) ? 0 : sizeTimeTimeUnit.hashCode());
  result=prime * result + ((sizeUnit == null) ? 0 : sizeUnit.hashCode());
  result=prime * result + ((timeUnit == null) ? 0 : timeUnit.hashCode());
  return result;
}
