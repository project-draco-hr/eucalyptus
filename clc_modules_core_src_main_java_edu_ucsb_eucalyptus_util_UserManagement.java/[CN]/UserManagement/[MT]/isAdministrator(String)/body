{
  try {
    if (Contexts.lookup().getUser().isSystemInternal() || ComponentIds.lookup(Eucalyptus.class).name().equals(userId) || "admin".equals(userId)) {
      return true;
    }
 else {
      return false;
    }
  }
 catch (  IllegalContextAccessException ex) {
    LOG.trace(ex,ex);
    return false;
  }
}
