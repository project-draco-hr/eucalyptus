{
  String id=ServletUtils.genGUID();
  long time=System.currentTimeMillis();
  WebSession session=new WebSession(id,userId,time,time);
  sessionsById.put(id,session);
  sessionsByUser.put(userId,session);
  return id;
}
