{
  Set<QueryValue> values=this.termMap.get(field);
  if (values == null || values.size() < 1) {
    return true;
  }
  for (  QueryValue v : values) {
    if (matcher.match(v)) {
      return true;
    }
  }
  return false;
}
