{
  final int prime=31;
  int result=1;
  result=prime * result + ((dimensionMap == null) ? 0 : dimensionMap.hashCode());
  result=prime * result + ((loadKey == null) ? 0 : loadKey.hashCode());
  result=prime * result + ((metricName == null) ? 0 : metricName.hashCode());
  result=prime * result + ((metricType == null) ? 0 : metricType.hashCode());
  return result;
}
