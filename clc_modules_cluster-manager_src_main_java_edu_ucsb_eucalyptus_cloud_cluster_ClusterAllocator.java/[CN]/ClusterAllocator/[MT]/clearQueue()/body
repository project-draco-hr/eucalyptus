{
  QueuedEvent event=null;
  while ((event=this.pendingEvents.poll()) != null) {
    Object o=null;
    try {
      o=event.getCallback().getResponse();
    }
 catch (    Throwable t) {
      LOG.debug(t,t);
    }
  }
}
