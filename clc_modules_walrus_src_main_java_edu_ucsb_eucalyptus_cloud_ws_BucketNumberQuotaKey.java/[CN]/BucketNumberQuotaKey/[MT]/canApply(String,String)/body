{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_S3,PolicySpec.S3_CREATEBUCKET).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_S3,PolicySpec.S3_RESOURCE_BUCKET).equals(resourceType)) {
    return true;
  }
  return false;
}
