{
  if (!Bootstrap.isOperational())   return false;
  final Record query=request.getQuery();
  if (DomainNames.isSystemSubdomain(query.getName()) && query.getName().toString().matches(".+\\.objectstorage\\..*"))   return true;
  return false;
}
