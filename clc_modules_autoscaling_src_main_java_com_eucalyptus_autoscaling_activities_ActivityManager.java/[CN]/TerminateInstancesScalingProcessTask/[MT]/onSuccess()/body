{
  return replace ? new LaunchInstancesScalingProcessTask(getGroup(),getTerminatedCount(),String.format("an instance was started in response to a difference between desired and actual capacity, increasing the capacity from %1$d to %2$d",getGroup().getCapacity() - getTerminatedCount(),getGroup().getCapacity())) : null;
}
