{
  return new Supplier<String>(){
    @Override public String get(){
      try {
        return Accounts.lookupAccountById(accountNumber).lookupUserByName(User.ACCOUNT_ADMIN).getUserId();
      }
 catch (      AuthException e) {
        throw Exceptions.toUndeclared(e);
      }
    }
  }
;
}
