{
  final EbsParameters ebs=blockDeviceMapping.getEbsParameters();
  return new BlockDeviceMappingType(blockDeviceMapping.getDeviceName(),blockDeviceMapping.getVirtualName(),ebs != null ? ebs.getSnapshotId() : null,ebs != null ? ebs.getVolumeSize() : null);
}
