{
  if (search != null) {
    LOG.log(Level.INFO,"New search: " + search);
    this.clientFactory.getMainHistorian().newItem(SearchPlace.encodeTyped(search),true);
  }
 else {
    LOG.log(Level.INFO,"Empty search!");
  }
}
