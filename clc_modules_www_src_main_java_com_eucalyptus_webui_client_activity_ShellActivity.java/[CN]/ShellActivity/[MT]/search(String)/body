{
  if (search != null) {
    LOG.log(Level.INFO,"New search: " + search);
    this.clientFactory.getMainHistorian().newItem(SearchPlace.encode(search),true);
  }
 else {
    LOG.log(Level.INFO,"Empty search!");
  }
}
