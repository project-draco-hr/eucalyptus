{
  final int prime=31;
  int result=1;
  result=prime * result + ((beginningMs == null) ? 0 : beginningMs.hashCode());
  result=prime * result + ((endingMs == null) ? 0 : endingMs.hashCode());
  return result;
}
