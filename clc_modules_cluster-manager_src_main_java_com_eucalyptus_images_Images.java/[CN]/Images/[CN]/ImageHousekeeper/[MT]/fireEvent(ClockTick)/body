{
  if (Topology.isEnabledLocally(Eucalyptus.class) && ready.compareAndSet(true,false)) {
    try {
      Threads.enqueue(Eucalyptus.class,ImageInfo.class,this);
    }
 catch (    final Exception ex) {
      ready.set(true);
    }
  }
}
