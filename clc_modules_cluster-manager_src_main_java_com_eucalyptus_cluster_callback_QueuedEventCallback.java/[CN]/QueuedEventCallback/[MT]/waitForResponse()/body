{
  this.canHas.lock();
  try {
    for (int i=0; i < 60 && this.response.get() == null; i++) {
      this.pollForResponse(1000l);
    }
  }
 catch (  InterruptedException e) {
    LOG.debug(e,e);
  }
 finally {
    this.canHas.unlock();
  }
}
