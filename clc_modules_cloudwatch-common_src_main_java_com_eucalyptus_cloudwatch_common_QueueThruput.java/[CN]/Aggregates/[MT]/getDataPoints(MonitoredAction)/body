{
  read.lock();
  List<DataPoint> ret;
  if (data.containsKey(action)) {
    ret=Collections.unmodifiableList(data.get(action));
  }
 else {
    ret=Collections.emptyList();
  }
  read.unlock();
  return ret;
}
