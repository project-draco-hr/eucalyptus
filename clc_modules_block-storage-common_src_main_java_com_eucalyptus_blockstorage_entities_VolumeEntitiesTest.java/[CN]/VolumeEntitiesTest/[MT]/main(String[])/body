{
  String volName="vol-fake";
  if (args.length > 1) {
    volName=args[1];
  }
  LOG.info("Testing add!");
  testAdd(volName);
  LOG.info("Testing remove!");
  testRemove(volName);
  LOG.info("Done with test!");
}
