{
  final int prime=31;
  int result=1;
  result=prime * result + getOuterType().hashCode();
  result=prime * result + ((critVal == null) ? 0 : critVal.hashCode());
  result=prime * result + ((groupByVal == null) ? 0 : groupByVal.hashCode());
  return result;
}
