{
  openTime.getAndSet(System.currentTimeMillis());
  LOG.debug(EventRecord.create(ctx.getPipeline().getLast().getClass().getSimpleName(),Component.eucalyptus.name(),"CONNECT",ctx.getChannel().getLocalAddress(),ctx.getChannel().getRemoteAddress().toString()));
  super.channelConnected(ctx,e);
}
