{
  List<String> eucaGroupIds=(List<String>)object;
  Attribute attr=null;
  for (  String id : eucaGroupIds) {
    if (id != null) {
      if (attr == null) {
        attr=new BasicAttribute(name);
      }
      attr.add(id);
    }
  }
  return attr;
}
