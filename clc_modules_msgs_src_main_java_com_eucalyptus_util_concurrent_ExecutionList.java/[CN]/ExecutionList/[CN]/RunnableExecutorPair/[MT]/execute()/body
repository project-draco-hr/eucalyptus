{
  try {
    EventRecord.here(runnable.getClass(),EventType.FUTURE,"run( )").debug();
    executor.execute(runnable);
  }
 catch (  RuntimeException e) {
    LOG.fatal("RuntimeException while executing runnable " + runnable + " with executor "+ executor,e);
  }
}
