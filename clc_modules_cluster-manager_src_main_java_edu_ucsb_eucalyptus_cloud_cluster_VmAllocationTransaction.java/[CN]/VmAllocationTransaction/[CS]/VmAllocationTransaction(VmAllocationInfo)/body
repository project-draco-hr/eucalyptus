{
  this.vmAllocInfo=vmAllocInfo;
  this.clusterMap=new ConcurrentHashMap<String,ClusterAllocator>();
  for (  ResourceToken token : this.vmAllocInfo.getAllocationTokens())   this.clusterMap.put(token.getCluster(),new ClusterAllocator(token,vmAllocInfo));
}
