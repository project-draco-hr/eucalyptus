{
  if (started) {
    try {
      brokerService.stop();
    }
 catch (    Exception ex) {
      throw new StartableException(ex);
    }
    started=false;
    log.info("Broker stopped");
  }
 else {
    log.warn("Broker stopped redundantly");
  }
}
