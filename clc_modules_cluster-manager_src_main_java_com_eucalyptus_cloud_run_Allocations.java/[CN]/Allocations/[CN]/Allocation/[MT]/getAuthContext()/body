{
  return context != null ? context.getAuthContext() : Permissions.createAuthContextSupplier(Accounts.lookupPrincipalByUserId(getOwnerFullName().getUserId(),null),Collections.<String,String>emptyMap());
}
