{
  return context != null ? context.getAuthContext() : Permissions.createAuthContextSupplier(Accounts.lookupUserById(getOwnerFullName().getUserId()),Lists.newArrayList(PolicyVersions.getAdministratorPolicy()),Collections.<String,String>emptyMap());
}
