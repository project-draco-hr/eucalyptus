{
  checkLocalDisk();
  if (isSecondary()) {
    return;
  }
  if (!isConnected()) {
    throw new EucalyptusCloudException("Resource not connected to peer.");
  }
  if (isMounted()) {
    unmountPrimary();
  }
  makeSecondary();
}
