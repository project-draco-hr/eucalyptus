{
  return requestUser.isSystemAdmin() || (requestUser.getAccountNumber().equals(account.getAccountNumber()) && Permissions.isAuthorized(requestUser.evaluationContext(PolicySpec.VENDOR_IAM,PolicySpec.IAM_RESOURCE_GROUP,PolicySpec.IAM_LISTGROUPS),account.getAccountNumber(),Accounts.getGroupFullName(group)));
}
