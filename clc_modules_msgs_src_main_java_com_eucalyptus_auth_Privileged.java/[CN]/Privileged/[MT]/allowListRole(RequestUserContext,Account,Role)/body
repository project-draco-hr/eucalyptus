{
  return requestUser.isSystemAdmin() || (requestUser.getAccountNumber().equals(account.getAccountNumber()) && Permissions.isAuthorized(requestUser.evaluationContext(PolicySpec.VENDOR_IAM,PolicySpec.IAM_RESOURCE_ROLE,PolicySpec.IAM_LISTROLES),account.getAccountNumber(),Accounts.getRoleFullName(role)));
}
