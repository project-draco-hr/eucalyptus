{
  final Account acct=requestUser.getAccount();
  if (!requestUser.isSystemAdmin()) {
    if (!Permissions.isAuthorized(PolicySpec.VENDOR_IAM,PolicySpec.IAM_DELETESERVERCERTIFICATE,certName,acct,PolicySpec.IAM_DELETESERVERCERTIFICATE,requestUser))     throw new AuthException(AuthException.ACCESS_DENIED);
  }
  try {
    acct.deleteServerCertificate(certName);
  }
 catch (  final AuthException ex) {
    throw ex;
  }
catch (  final Exception ex) {
    throw ex;
  }
}
