{
  try {
    return Transactions.filter(LaunchConfiguration.withOwner(ownerFullName),filter);
  }
 catch (  Exception e) {
    throw new MetadataException("Failed to find launch configurations for " + ownerFullName,e);
  }
}
