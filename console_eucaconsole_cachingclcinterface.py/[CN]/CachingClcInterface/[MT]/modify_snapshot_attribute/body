def modify_snapshot_attribute(self, snapshot_id, attribute, operation, users, groups, callback):
    self.snapshots.expireCache()
    Threads.instance().runThread(self.__modify_snapshot_attribute_cb__, ({'snapshot_id': snapshot_id, 'attribute': attribute, 'operation': operation, 'user': user, 'groups': groups, }, callback))
