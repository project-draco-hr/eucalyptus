def create_security_group(self, name, description, callback):
    self.caches['groups'].expireCache()
    Threads.instance().runThread(self.__create_security_group_cb__, ({'name': name, 'description': description, }, callback))
