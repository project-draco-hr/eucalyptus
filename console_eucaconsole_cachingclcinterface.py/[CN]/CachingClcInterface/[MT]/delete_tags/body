def delete_tags(self, resourceIds, tags, callback):
    self.caches['tags'].expireCache()
    Threads.instance().runThread(self.__delete_tag_cb__, ({'resource_ids': resourceIds, 'tags': tags, }, callback))
