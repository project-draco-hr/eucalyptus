def reset_snapshot_attribute(self, snapshot_id, attribute, callback):
    self.snapshots.expireCache()
    Threads.instance().runThread(self.__reset_snapshot_attribute_cb__, ({'snapshot_id': snapshot_id, 'attribute': attribute, }, callback))
