def start_instances(self, instance_ids, callback):
    self.caches['instances'].expireCache()
    Threads.instance().runThread(self.__start_instances_cb__, ({'instance_ids': instance_ids, }, callback))
