def modify_image_attribute(self, image_id, attribute, operation, users, groups):
    self.images.expireCache()
    Threads.instance().runThread(self.__modify_image_attribute_cb__, ({'image_id': image_id, 'attribute': attribute, 'operation': operation, 'users': users, 'groups': groups, }, callback))
