def modify_image_attribute(self, image_id, attribute, operation, users, groups):
    self.imageUpdate = datetime.min
    return self.clc.modify_image_attribute(image_id, attribute, operation, users, groups)
