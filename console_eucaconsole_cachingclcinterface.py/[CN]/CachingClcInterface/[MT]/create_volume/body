def create_volume(self, size, availability_zone, snapshot_id, callback):
    self.volumes.expireCache()
    Threads.instance().runThread(self.__get_all_volumes_cb__, ({'size': size, 'availability_zone': availability_zone, 'snapshot_id': snapshot_id, }, callback))
