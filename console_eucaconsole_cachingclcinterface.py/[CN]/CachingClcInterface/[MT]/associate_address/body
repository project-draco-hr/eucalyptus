def associate_address(self, publicip, instanceid, callback):
    self.addresses.expireCache()
    Threads.instance().runThread(self.__release_address_cb__, ({'publicip': publicip, 'instanceid': instanceid, }, callback))
