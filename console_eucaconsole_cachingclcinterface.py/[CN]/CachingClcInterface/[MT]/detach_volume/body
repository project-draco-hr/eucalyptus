def detach_volume(self, volume_id, force=False, callback=None):
    self.volumes.expireCache()
    Threads.instance().runThread(self.__detach_volume_cb__, ({'volume_id': volume_id, 'force': force, }, callback))
