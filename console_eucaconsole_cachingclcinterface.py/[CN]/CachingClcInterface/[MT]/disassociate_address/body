def disassociate_address(self, publicip, callback):
    self.caches['addresses'].expireCache()
    Threads.instance().runThread(self.__disassociate_address_cb__, ({'publicip': publicip, }, callback))
