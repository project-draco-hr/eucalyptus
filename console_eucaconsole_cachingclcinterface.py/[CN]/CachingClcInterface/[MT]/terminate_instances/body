def terminate_instances(self, instance_ids, callback):
    self.instances.expireCache()
    Threads.instance().runThread(self.__terminate_instances_cb__, ({'instance_ids': instance_ids, }, callback))
