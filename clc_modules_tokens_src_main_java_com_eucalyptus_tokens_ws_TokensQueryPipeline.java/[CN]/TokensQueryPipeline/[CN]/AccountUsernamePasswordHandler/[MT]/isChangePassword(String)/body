{
  boolean changePassword=false;
  if ((unEncodedAuth.indexOf(";") > -1)) {
    if (unEncodedAuth.indexOf(":") == -1 || !(unEncodedAuth.indexOf(":") < unEncodedAuth.indexOf(";")))     changePassword=true;
  }
  return changePassword;
}
