{
  final Map<String,S3UsageSummary> resultMap=new HashMap<String,S3UsageSummary>();
  for (  String key : infoMap.keySet()) {
    resultMap.put(key,infoMap.get(key).getSummary());
  }
  return resultMap;
}
