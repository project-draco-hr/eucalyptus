{
  if (this.disabledMap.containsKey(obj.getName())) {
    TYPE tempObj=this.disabledMap.remove(obj.getName());
    this.activeMap.putIfAbsent(tempObj.getName(),tempObj);
  }
 else   this.activeMap.putIfAbsent(obj.getName(),obj);
}
