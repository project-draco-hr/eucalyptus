{
  if (this.activeMap.containsKey(obj.getName())) {
    TYPE tempObj=this.activeMap.remove(obj.getName());
    this.disabledMap.putIfAbsent(tempObj.getName(),tempObj);
  }
 else   this.disabledMap.putIfAbsent(obj.getName(),obj);
}
