{
  if (!this.disabledMap.containsKey(name))   throw new NoSuchElementException("Can't find registered object: " + name + " in "+ this.getClass().getSimpleName());
  TYPE obj=this.disabledMap.remove(name);
  this.activeMap.putIfAbsent(obj.getName(),obj);
}
