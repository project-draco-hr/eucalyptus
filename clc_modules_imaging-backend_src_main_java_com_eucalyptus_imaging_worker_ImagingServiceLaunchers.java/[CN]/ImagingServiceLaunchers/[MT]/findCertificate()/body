{
  try {
    return EuareClient.getInstance().describeServerCertificate(Accounts.lookupImagingAccount().getUserId(),SERVER_CERTIFICATE_NAME,DEFAULT_SERVER_CERT_PATH);
  }
 catch (  Exception ex) {
    throw new EucalyptusCloudException("failed to describe server cert",ex);
  }
}
