{
  return new Predicate<Component>(){
    @Override public boolean apply(    Component input){
      return Any.class.equals(compId.getClass()) || input.getComponentId().equals(compId);
    }
  }
;
}
