{
  this.transition(State.allocated,State.impending,false,true,new SplitTransition(Transition.system){
    public void bottom(){
      Address.this.state.set(State.allocated,false);
    }
    public void top(){
    }
  }
);
  return this;
}
