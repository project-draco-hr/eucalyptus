{
  this.transition(State.allocated,State.assigned,false,true,new GuardedTransition<Transition>(Transition.assigning){
    public void apply(){
      Address.this.setInstanceId(instanceId);
      Address.this.setInstanceAddress(instanceAddr);
    }
  }
,QUIESCENT);
  return this;
}
