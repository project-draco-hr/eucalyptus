{
  return "Address " + this.getDisplayName() + " "+ this.partition+ " "+ (this.isAllocated() ? this.getOwner() + " " : "")+ (this.isAssigned() ? this.instanceId + " " + this.instanceAddress+ " " : "")+ " "+ this.transition;
}
