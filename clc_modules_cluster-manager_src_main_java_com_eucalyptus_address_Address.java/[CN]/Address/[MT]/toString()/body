{
  return "Address " + this.name + " "+ this.cluster+ " "+ (this.isAllocated() ? this.userId + " " : "")+ (this.isAssigned() ? this.instanceId + " " + this.instanceAddress+ " " : "")+ " "+ this.state.getReference()+ (this.state.isMarked() ? ":pending" : "")+ this.transition;
}
