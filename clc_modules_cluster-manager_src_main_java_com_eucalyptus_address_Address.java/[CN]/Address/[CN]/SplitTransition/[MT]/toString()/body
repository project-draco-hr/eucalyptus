{
  State curr=Address.this.atomicState.getReference();
  boolean mark=Address.this.atomicState.isMarked();
  return String.format("AddressTransition %s:%s(%s)",this.t,this.previous != curr ? this.previous + "->" + curr : this.previous,mark);
}
