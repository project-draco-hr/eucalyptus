{
  return String.format("[SplitTransition previous=%s, transition=%s, next=%s, pending=%s, transitionId=%s]",this.previous,this.t,Address.this.state.getReference(),Address.this.state.isMarked(),Address.this.transitionId);
}
