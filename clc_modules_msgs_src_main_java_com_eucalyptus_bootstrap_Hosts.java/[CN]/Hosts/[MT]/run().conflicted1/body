{
  if (Hosts.pruneHosts()) {
    Hosts.updateServices();
  }
  for (  Host h : Hosts.listDatabases()) {
    if (!h.isLocalHost() && h.hasSynced()) {
      Databases.enable(h);
    }
 else     if (h.isLocalHost()) {
      Databases.enable(h);
    }
  }
}
