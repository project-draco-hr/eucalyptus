{
  final Host localHost=Hosts.localHost();
  boolean ret=h.hasDatabase() && (h.getStartedTime() <= localHost.getStartedTime());
  LOG.debug("Is coordinator=" + ret + " for host: "+ h);
  return ret;
}
