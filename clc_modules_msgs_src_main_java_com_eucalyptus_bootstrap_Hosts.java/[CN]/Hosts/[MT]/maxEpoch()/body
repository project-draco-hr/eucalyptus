{
  try {
    return Collections.max(Collections2.transform(hostMap.values(),new Function<Host,Integer>(){
      @Override public Integer apply(      Host arg0){
        return arg0.getEpoch();
      }
    }
));
  }
 catch (  Exception ex) {
    return 0;
  }
}
