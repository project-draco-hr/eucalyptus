{
  Host ret=null;
  try {
    ret=Iterables.find(input,this);
  }
 catch (  final NoSuchElementException ex) {
    ret=Hosts.localHost();
  }
  LOG.debug("Found coordinator: " + ret);
  return ret;
}
