{
  try {
    ListenerRegistry.getInstance().fireEvent(SnapShotEvent.with(actionInfo,snap.getNaturalId(),snap.getDisplayName(),snap.getOwner()));
  }
 catch (  final Exception e) {
    LOG.error(e,e);
  }
}
