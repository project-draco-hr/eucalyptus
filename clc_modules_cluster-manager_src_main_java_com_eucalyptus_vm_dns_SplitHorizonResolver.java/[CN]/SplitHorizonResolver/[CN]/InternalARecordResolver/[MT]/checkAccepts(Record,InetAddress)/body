{
  return RequestType.PTR.apply(query) ? super.checkAccepts(query,source) : super.checkAccepts(query,source) && (InstanceDomainNames.isInstanceSubdomain(query.getName()) && !query.getName().subdomain(InstanceDomainNames.EXTERNAL.get()));
}
