{
  return RequestType.PTR.apply(query) ? super.checkAccepts(query,source) : super.checkAccepts(query,source) && !Subnets.isSystemManagedAddress(source) && query.getName().subdomain(InstanceDomainNames.EXTERNAL.get());
}
