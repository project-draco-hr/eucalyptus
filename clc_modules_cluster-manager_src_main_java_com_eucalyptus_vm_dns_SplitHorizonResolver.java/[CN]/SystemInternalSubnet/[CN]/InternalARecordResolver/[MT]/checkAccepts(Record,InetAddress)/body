{
  return super.checkAccepts(query,source) && RequestType.A.apply(query) && DomainNames.isInternalSubdomain(query.getName())&& InstanceDomainName.matcher(query.getName()).matches()&& query.getName().subdomain(DomainNames.internalSubdomain(Eucalyptus.class));
}
