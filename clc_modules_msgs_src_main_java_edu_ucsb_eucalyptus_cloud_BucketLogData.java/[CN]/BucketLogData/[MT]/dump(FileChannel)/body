{
  try {
    channel.write(ByteBuffer.wrap(String.format(ENTRY_FORMAT,getOwnerId(),getBucketName(),"[" + getTimestamp() + "]",getSourceAddress(),getAccessorId(),getRequestId(),getOperation(),getKey(),getUri(),getStatus(),getError(),getBytesSent(),getObjectSize(),getTotalTime(),getTurnAroundTime(),getReferrer(),getUserAgent()).getBytes()));
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
