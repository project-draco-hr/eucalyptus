{
  NodeInfo ret=null;
  for (  NodeLogInfo log : nodeCerts)   if ((ret=this.nodeMap.putIfAbsent(log.getServiceTag(),new NodeInfo(log))) != null)   this.nodeMap.replace(log.getServiceTag(),new NodeInfo(ret,log));
}
