{
  NodeInfo ret=null;
  for (  NodeCertInfo cert : nodeCerts)   if ((ret=this.nodeMap.putIfAbsent(cert.getServiceTag(),new NodeInfo(cert))) != null)   this.nodeMap.replace(cert.getServiceTag(),new NodeInfo(ret,cert));
}
