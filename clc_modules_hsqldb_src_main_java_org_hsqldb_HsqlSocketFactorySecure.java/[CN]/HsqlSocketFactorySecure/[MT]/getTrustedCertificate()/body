{
  try {
synchronized (HsqlSocketFactorySecure.class) {
      if (trusted == null) {
        trusted=cert;
        trustedKey=pk;
      }
      return trusted;
    }
  }
 catch (  Exception e) {
    LOG.error(e,e);
    throw new RuntimeException(e);
  }
}
