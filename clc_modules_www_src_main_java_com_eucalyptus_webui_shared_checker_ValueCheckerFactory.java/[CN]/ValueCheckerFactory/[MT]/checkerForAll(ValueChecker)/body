{
  return new ValueChecker(){
    @Override public String check(    final String value) throws InvalidValueException {
      String currentValue=value;
      for (      final ValueChecker checker : checkers) {
        currentValue=checker == null ? value : checker.check(value);
      }
      return currentValue;
    }
  }
;
}
