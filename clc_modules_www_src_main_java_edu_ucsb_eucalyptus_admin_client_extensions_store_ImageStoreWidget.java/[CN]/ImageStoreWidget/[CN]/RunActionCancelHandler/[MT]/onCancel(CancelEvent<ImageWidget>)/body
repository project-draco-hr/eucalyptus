{
  ImageWidget targetWidget=event.getTarget();
  ImageState imageState=targetWidget.getImageState();
  client.runAction(imageState,ImageState.Action.CANCEL,new ActionResponseHandler(targetWidget));
}
