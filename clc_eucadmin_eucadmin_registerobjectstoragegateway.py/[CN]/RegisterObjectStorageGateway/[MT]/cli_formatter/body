def cli_formatter(self, data):
    eucadmin.registerrequest.RegisterRequest.cli_formatter(self, data)
    if (not self.osg_preexists):
        print  >> sys.stderr, 'Registered the first Object Storage Gateway. You must choose a object storage back end client with ``euca-modify-property -p objectstorage.providerclient=$PROVIDER (e.g. walrus or s3)'
