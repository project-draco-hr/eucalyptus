{
  try {
    final PROTOCOL protocol=PROTOCOL.valueOf(listener.getProtocol().toUpperCase());
    if (PROTOCOL.HTTP.equals(protocol) || PROTOCOL.TCP.equals(protocol) || PROTOCOL.HTTPS.equals(protocol)|| PROTOCOL.SSL.equals(protocol))     return true;
 else     return false;
  }
 catch (  Exception e) {
    return false;
  }
}
