{
  return (long)Iterables.size(Iterables.filter(Addresses.getInstance().listValues(),Predicates.and(RestrictedTypes.filterByOwner(input),Predicates.compose(Predicates.equalTo(Boolean.TRUE),BooleanFilterFunctions.IS_ALLOCATED))));
}
