{
  if (msgId != null) {
    String requestId=null;
    if (!msgId.contains("::"))     requestId=msgId;
 else     requestId=msgId.substring(0,msgId.indexOf("::"));
    this.correlationId=String.format("%s::%s",requestId,UUID.randomUUID().toString());
  }
  return (TYPE)this;
}
