{
  if (requestId != null) {
    if (this.correlationId == null)     this.correlationId=String.format("%s::%s",requestId,UUID.randomUUID().toString());
 else     this.correlationId=String.format("%s::%s",requestId,this.correlationId);
  }
  return (TYPE)this;
}
