{
  final long gracePeriodMillis=TimeUnit.SECONDS.toMillis(Objects.firstNonNull(getAutoScalingGroup().getHealthCheckGracePeriod(),300));
  return System.currentTimeMillis() - getCreationTimestamp().getTime() > gracePeriodMillis;
}
