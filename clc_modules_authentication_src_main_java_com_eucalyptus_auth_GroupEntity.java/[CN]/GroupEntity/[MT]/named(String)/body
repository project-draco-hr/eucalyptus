{
  EntityWrapper<GroupEntity> db=new EntityWrapper<GroupEntity>();
  GroupEntity userGroup=null;
  try {
    userGroup=db.getUnique(new GroupEntity(name));
    db.commit();
  }
 catch (  Exception e) {
    db.rollback();
    throw new EucalyptusCloudException(e);
  }
  return userGroup;
}
