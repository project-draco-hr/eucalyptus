{
  return new Predicate<ConditionOp>(){
    @Override public boolean apply(    final ConditionOp conditionOp){
      System.out.println(conditionOp);
      return !conditionOp.check(key,value);
    }
  }
;
}
