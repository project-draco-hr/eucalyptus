{
  URL url=new URL(req.getRequestURL().toString());
  return String.format("https://%s:%d/%s",redirectHost,HttpServerBootstrapper.HTTPS_PORT,url.getPath() + ((req.getQueryString() != null) ? "?" + req.getQueryString() : ""));
}
