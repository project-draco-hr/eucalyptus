{
  try {
    SANInfo sanInfo=SANInfo.getStorageInfo();
    if (!StorageProperties.DUMMY_SAN_HOST.equals(sanInfo.getSanHost()))     sessionManager.checkConnection();
  }
 catch (  EucalyptusCloudException e) {
    enabled=false;
    return;
  }
  addUser(TARGET_USERNAME);
}
