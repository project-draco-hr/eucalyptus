{
  try {
    if (!StorageProperties.DUMMY_SAN_HOST.equals(host))     sessionManager.checkConnection();
  }
 catch (  EucalyptusCloudException e) {
    enabled=false;
    return;
  }
  addUser(TARGET_USERNAME);
}
