{
  NaElement request=makeRequest("lun-map","initiator-group",igroupName,"path",lunPath);
  NaElement reply=execCommand(request);
  return Integer.parseInt(reply.getChildContent("lun-id-assigned"));
}
