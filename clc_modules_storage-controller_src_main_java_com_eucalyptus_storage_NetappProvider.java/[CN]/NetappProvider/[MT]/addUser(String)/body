{
  try {
    NaElement request=makeRequest("iscsi-initiator-set-default-auth","auth-type","deny");
    NaElement reply=execCommand(request);
  }
 catch (  EucalyptusCloudException e) {
    LOG.error("Unable to add default deny all " + e);
  }
}
