def get_extant_src_files(self):
    found = []
    for src_file in self.src_files:
        if os.path.isfile(src_file):
            found.append(src_file)
        else:
            self.warn(('unable to sync file %s because it does not exist; services may have trouble communicating' % src_file))
    return found
