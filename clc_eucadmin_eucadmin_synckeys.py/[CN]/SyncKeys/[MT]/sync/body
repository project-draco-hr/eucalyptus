def sync(self):
    if self.local:
        self.sync_local()
        return True
    else:
        if self.sync_rsync():
            return True
        if self.sync_scp():
            return True
        return False
