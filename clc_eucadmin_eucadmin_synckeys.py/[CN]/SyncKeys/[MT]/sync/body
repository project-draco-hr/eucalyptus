def sync(self):
    if self.local:
        self.sync_local()
        return True
    else:
        if (self.use_rysync and self.sync_rsync()):
            return True
        if (self.use_scp and self.sync_scp()):
            return True
        return False
