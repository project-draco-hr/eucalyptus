def sync_local(self, src_files):
    for src_file in src_files:
        try:
            shutil.copy2(src_file, self.dst_dir)
        except Exception as exc:
            self.error(('failed to copy %s to %s: %s' % (src_file, self.dst_dir, str(exc))))
            return False
    return True
