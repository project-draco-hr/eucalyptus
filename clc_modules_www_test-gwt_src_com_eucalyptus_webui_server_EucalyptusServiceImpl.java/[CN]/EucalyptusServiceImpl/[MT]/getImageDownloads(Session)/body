{
  return new ArrayList<DownloadInfo>(Arrays.asList(new DownloadInfo("http://localhost","kernel","Kernel image"),new DownloadInfo("http://localhost","ramdisk","Ramdisk image"),new DownloadInfo("http://localhost","root","Root disk image")));
}
