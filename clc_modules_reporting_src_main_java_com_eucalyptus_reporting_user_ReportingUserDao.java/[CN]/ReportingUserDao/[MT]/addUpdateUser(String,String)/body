{
  loadIfNeededFromDb();
  if (users.containsKey(id) && users.get(id).equals(name)) {
    return;
  }
 else   if (users.containsKey(id)) {
    users.put(id,name);
    updateInDb(id,name);
  }
 else {
    users.put(id,name);
    addToDb(id,name);
  }
}
