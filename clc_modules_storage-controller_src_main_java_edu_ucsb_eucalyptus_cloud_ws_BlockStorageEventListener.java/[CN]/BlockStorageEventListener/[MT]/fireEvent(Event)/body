{
  if (event instanceof StartComponentEvent) {
    if (((StartComponentEvent)event).isLocal())     try {
      BlockStorage.configure();
    }
 catch (    EucalyptusCloudException e) {
      LOG.error(e);
    }
  }
}
