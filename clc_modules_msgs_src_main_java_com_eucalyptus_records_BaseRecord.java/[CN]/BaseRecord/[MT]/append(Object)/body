{
  for (  Object o : obj) {
    this.others.add(o == null ? ISNULL : "" + o);
  }
  this.extra="";
  for (  Object o : this.others) {
    if (o == null)     continue;
    this.extra+=":" + o.toString();
  }
  return this;
}
