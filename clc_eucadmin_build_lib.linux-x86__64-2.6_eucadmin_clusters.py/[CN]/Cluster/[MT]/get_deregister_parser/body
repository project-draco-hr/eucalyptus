def get_deregister_parser(self):
    self.parser = OptionParser('usage: %prog [options] CLUSTERNAME', version='Eucalyptus %prog VERSION')
    self.parser.add_option('-P', '--partition', dest='partition', help='Partition for the cluster.')
    (options, args) = self.parser.parse_args()
    if (len(args) != 1):
        s = 'ERROR  Required argument CLUSTERNAME is missing or malformed.'
        self.error(s)
    else:
        return (options, args)
