{
  if (!contains(type,name)) {
    throw new NoSuchElementException("Missing entry for component '" + name + "' info type: "+ type.getSimpleName()+ " ("+ getRealType(type).getCanonicalName());
  }
 else {
    return (T)Components.lookup(type).get(name);
  }
}
