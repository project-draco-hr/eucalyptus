{
  if (dispatcherPrinter != null) {
    return dispatcherPrinter;
  }
 else {
synchronized (Components.class) {
      return new Callback.Success<Dispatcher>(){
        @Override public void fire(        Dispatcher arg0){
          LOG.info(dispatcherToString.apply(arg0));
        }
      }
;
    }
  }
}
