{
  return config.getComponentId().name() + (config.getPartition() == null || config.getComponentId().name().equals(config.getPartition()) || config.getName().equals(config.getPartition()) ? "" : "-" + config.getPartition()) + "-"+ config.getName();
}
