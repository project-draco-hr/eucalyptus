{
  if (PolicySpec.qualifiedName(PolicySpec.VENDOR_S3,PolicySpec.S3_PUTOBJECT).equals(action) && PolicySpec.qualifiedName(PolicySpec.VENDOR_S3,PolicySpec.S3_RESOURCE_OBJECT).equals(resourceType)) {
    return true;
  }
  return false;
}
