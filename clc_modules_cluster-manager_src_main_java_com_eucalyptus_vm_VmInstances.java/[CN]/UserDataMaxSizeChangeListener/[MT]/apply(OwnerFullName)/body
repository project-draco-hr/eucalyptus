{
  final EntityTransaction db=Entities.get(VmInstance.class);
  final long i;
  try {
    i=Entities.count(VmInstance.named(input,null),Restrictions.not(criterion(VmStateSet.DONE.array())),Collections.<String,String>emptyMap());
  }
  finally {
    db.rollback();
  }
  return i;
}
