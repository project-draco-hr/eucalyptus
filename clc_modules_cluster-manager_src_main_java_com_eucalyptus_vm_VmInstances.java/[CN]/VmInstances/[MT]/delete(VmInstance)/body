{
  try {
    if (VmStateSet.DONE.apply(vm)) {
      cache(vm);
      return VmInstance.Transitions.DELETE.apply(vm);
    }
  }
 catch (  final Exception ex) {
    LOG.error(ex,ex);
  }
  return vm;
}
