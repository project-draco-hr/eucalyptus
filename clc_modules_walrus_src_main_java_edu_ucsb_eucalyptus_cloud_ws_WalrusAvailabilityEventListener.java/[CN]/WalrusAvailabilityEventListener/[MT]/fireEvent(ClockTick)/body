{
  if (Bootstrap.isFinished() && Hosts.isCoordinator()) {
    try {
      ListenerRegistry.getInstance().fireEvent(new ResourceAvailabilityEvent(StorageWalrus,new Availability(0,WalrusUtil.countTotalObjectSize())));
    }
 catch (    Exception ex) {
      logger.error(ex,ex);
    }
  }
}
