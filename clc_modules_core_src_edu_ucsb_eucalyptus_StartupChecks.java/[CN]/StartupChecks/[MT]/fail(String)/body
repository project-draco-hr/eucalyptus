{
  printFail();
  for (  String s : error)   LOG.fatal(s);
  if (EntityWrapper.getEntityManagerFactory(EucalyptusProperties.NAME).isOpen())   EntityWrapper.getEntityManagerFactory(EucalyptusProperties.NAME).close();
  System.exit(0xEC2);
}
