{
  List<ClusterInfoWeb> clusterList=new ArrayList<ClusterInfoWeb>();
  for (  RegisterClusterType c : Clusters.getInstance().getClusters())   clusterList.add(new ClusterInfoWeb(c.getName(),c.getHost(),c.getPort()));
  return clusterList;
}
