{
  return new MessageCallback<CreateImageType,CreateImageResponseType>(){
{
      this.setRequest(request);
    }
    @Override public void fire(    CreateImageResponseType msg){
      LOG.info(msg);
    }
  }
;
}
