{
  String namespace="";
  final ComponentMessage componentMessage=Ats.inClassHierarchy(klass).get(ComponentMessage.class);
  if (componentMessage != null) {
    namespace+=Classes.newInstance(componentMessage.value()).name();
  }
 else {
    namespace+="euca";
  }
  return namespace;
}
