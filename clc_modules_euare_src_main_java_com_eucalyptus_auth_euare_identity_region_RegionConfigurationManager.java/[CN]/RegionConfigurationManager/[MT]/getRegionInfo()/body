{
  return Iterables.tryFind(Iterables.concat(regionConfigurationSupplier.get().asSet()),propertyPredicate(RegionConfigurations.getRegionName().asSet(),RegionNameTransform.INSTANCE)).transform(TypeMappers.lookup(Region.class,RegionInfo.class));
}
