{
  ArbitratorConfiguration arbConfig=(ArbitratorConfiguration)serviceConf;
  int i=COMMON_FIELD_DESCS.size();
  try {
    String val=input.getField(i++);
    arbConfig.setGatewayHost(val);
  }
 catch (  Exception e) {
    LOG.error(e,e);
  }
}
