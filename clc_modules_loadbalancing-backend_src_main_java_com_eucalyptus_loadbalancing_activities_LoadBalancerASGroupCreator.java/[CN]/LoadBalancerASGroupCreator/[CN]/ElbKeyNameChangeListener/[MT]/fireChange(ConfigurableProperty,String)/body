{
  try {
    if (t.getValue() != null && !t.getValue().equals(newValue))     onPropertyChange(null,null,newValue,null);
  }
 catch (  final Exception e) {
    throw new ConfigurablePropertyException("Could not change key name due to: " + e.getMessage());
  }
}
