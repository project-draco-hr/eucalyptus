{
  LOG.debug("Checking");
  try {
    if (httpHeadToRootReturnCode() != excuteHeadRequest(this.getUpstreamEndpoint()))     throw new EucalyptusCloudException("Unable to connect to S3 Endpoint Please check configuration and network connection");
  }
 catch (  URISyntaxException e) {
    throw new EucalyptusCloudException("Unable to connect to S3 Endpoint Please check configuration and network connection");
  }
  LOG.debug("Check completed successfully");
}
