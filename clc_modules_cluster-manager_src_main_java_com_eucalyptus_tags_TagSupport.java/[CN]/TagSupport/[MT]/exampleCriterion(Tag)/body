{
  final DetachedCriteria detachedCriteria=DetachedCriteria.forClass(resourceClass).add(Restrictions.in(resourceClassIdField,Lists.newArrayList(example.getResourceId()))).setProjection(Projections.id());
  return Property.forName(tagClassResourceField).in(detachedCriteria);
}
