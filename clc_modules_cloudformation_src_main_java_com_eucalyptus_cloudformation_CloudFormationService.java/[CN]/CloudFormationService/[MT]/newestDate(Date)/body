{
  Date latestDate=null;
  for (  Date date : dates) {
    if (date == null)     continue;
    if (date != null && latestDate == null) {
      latestDate=date;
      continue;
    }
    if (date.after(latestDate)) {
      latestDate=date;
    }
  }
  return latestDate;
}
