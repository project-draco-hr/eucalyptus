{
  if (imageData.info != null && imageData.state != null && (imageData.rowIndex != -1 || imageData.state.getStatus().isTransient() || imageData.state.getErrorMessage() != null)) {
    updateGrid(imageData);
    if (isAttached()) {
      updateDisplay();
    }
  }
}
