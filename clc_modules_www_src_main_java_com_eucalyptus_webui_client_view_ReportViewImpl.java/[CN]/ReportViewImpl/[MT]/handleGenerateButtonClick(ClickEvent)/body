{
  this.contentPanel.clear();
  this.contentPanel.add(loadingAnimation);
  this.presenter.generateReport(fromDate.getValue(),toDate.getValue(),criteria.getValue(criteria.getSelectedIndex()),groupBy.getValue(groupBy.getSelectedIndex()),type.getValue(type.getSelectedIndex()));
}
