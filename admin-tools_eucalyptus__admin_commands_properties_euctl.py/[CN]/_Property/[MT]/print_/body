def print_(self, suppress_name=False, force=False):
    if (self.value == {}):
        value = ''
    else:
        value = self.value
    if suppress_name:
        print value.strip()
    else:
        print '{0} = {1}'.format(self.name.strip(), value.strip())
