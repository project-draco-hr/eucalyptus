{
  if (ctx != null) {
    return ctx;
  }
 else {
synchronized (EmpyreanTransactionManager.class) {
      if (ctx == null) {
        tm=configureTm();
        ctx=new BitronixContext();
      }
      return ctx;
    }
  }
}
