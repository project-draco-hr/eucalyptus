{
  if (instance.zone != null)   zone=TypeMappers.transform(instance.zone,LoadBalancerZoneCoreView.class);
  if (instance.loadbalancer != null) {
    Entities.initialize(instance.loadbalancer);
    loadbalancer=instance.loadbalancer;
  }
}
