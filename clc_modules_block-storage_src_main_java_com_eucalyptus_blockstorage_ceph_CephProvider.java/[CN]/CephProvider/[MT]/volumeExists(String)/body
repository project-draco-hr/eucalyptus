{
  try {
    if (null != rbdService.getImagePool(volumeId)) {
      return true;
    }
 else {
      return false;
    }
  }
 catch (  Exception e) {
    LOG.debug("Caught error in check for " + volumeId,e);
    return false;
  }
}
