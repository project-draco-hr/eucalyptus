{
  return StringUtils.equals(configFile,info.getCephConfigFile()) && StringUtils.equals(user,info.getCephUser()) && StringUtils.equals(volumePools,info.getCephVolumePools())&& StringUtils.equals(snapshotPools,info.getCephSnapshotPools());
}
