{
  assertThat("Protocol must be one of: " + Protocol.values(),Lists.transform(Arrays.asList(Protocol.values()),Functions.toStringFunction()),contains(protocol));
  return create(Protocol.valueOf(protocol),lowPort,highPort,peers,ipRanges);
}
