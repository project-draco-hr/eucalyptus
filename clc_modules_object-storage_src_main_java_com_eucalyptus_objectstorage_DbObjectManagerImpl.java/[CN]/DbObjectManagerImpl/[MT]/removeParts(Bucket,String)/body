{
  try (TransactionResource db=Entities.transactionFor(ObjectEntity.class)){
    Entities.deleteAllMatching(ObjectEntity.class,"where part_number IS NOT NULL and upload_id=:uploadId",Collections.singletonMap("uploadId",uploadId));
    db.commit();
  }
 }
