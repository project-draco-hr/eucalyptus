{
  return enabled && (RequestType.A.apply(query) || RequestType.CNAME.apply(query)) && DomainNames.isSystemSubdomain(query.getName())&& (ResolverSupport.COMPONENT.apply(query.getName()) || ResolverSupport.SERVICE.apply(query.getName()));
}
