{
  canHas.lock();
  try {
    if (serverSocketFactory == null) {
      serverSocketFactory=new NioClientSocketChannelFactory(sharedBossPool,clientThreadPool,Runtime.getRuntime().availableProcessors() * (2 + 1));
    }
  }
  finally {
    canHas.unlock();
  }
  return serverSocketFactory;
}
