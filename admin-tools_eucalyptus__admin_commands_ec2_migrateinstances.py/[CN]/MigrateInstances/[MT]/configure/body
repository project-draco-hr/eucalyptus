def configure(self):
    EC2Request.configure(self)
    if (self.args.get('stop_source') and (not self.args.get('source'))):
        raise ArgumentError('argument --stop-source: only allowed with -s/--source')
