{
  RLOCK.lock();
  try {
    if (pool != null) {
      LOG.info("Shutting down SC thread pool catering to snapshot creation");
      pool.shutdownNow();
      pool=null;
    }
  }
  finally {
    RLOCK.unlock();
  }
}
