{
  String searchId=null;
  if (this.isSet(userSuppliedId))   searchId=userSuppliedId;
 else   if (this.isSet(imageDefaultId))   searchId=imageDefaultId;
 else   if (this.isSet(systemDefaultId))   searchId=systemDefaultId;
  return searchId;
}
